-- MySQL dump 10.13  Distrib 8.0.46, for Win64 (x86_64)
--
-- Host: 127.0.0.1    Database: RAG
-- ------------------------------------------------------
-- Server version	8.0.46

/*!40101 SET @OLD_CHARACTER_SET_CLIENT=@@CHARACTER_SET_CLIENT */;
/*!40101 SET @OLD_CHARACTER_SET_RESULTS=@@CHARACTER_SET_RESULTS */;
/*!40101 SET @OLD_COLLATION_CONNECTION=@@COLLATION_CONNECTION */;
/*!50503 SET NAMES utf8mb4 */;
/*!40103 SET @OLD_TIME_ZONE=@@TIME_ZONE */;
/*!40103 SET TIME_ZONE='+00:00' */;
/*!40014 SET @OLD_UNIQUE_CHECKS=@@UNIQUE_CHECKS, UNIQUE_CHECKS=0 */;
/*!40014 SET @OLD_FOREIGN_KEY_CHECKS=@@FOREIGN_KEY_CHECKS, FOREIGN_KEY_CHECKS=0 */;
/*!40101 SET @OLD_SQL_MODE=@@SQL_MODE, SQL_MODE='NO_AUTO_VALUE_ON_ZERO' */;
/*!40111 SET @OLD_SQL_NOTES=@@SQL_NOTES, SQL_NOTES=0 */;

--
-- Dumping data for table `knowledge`
--

LOCK TABLES `knowledge` WRITE;
/*!40000 ALTER TABLE `knowledge` DISABLE KEYS */;
INSERT INTO `knowledge` (`id`, `title`, `content`, `source_url`, `category`, `source_type`, `status`, `created_at`, `updated_at`, `user_id`, `quality_score`, `quality_reason`) VALUES (239,'Python 变量与基本数据类型','# Python 变量与基本数据类型\n\n## 是什么\n\n**大白话**：变量就是一个贴了名字标签的盒子，往里面装数据。Python 不要求你提前声明盒子里装什么，装的时候它自己认；而且同一个盒子今天装数字、明天装文字都行。基本数据类型就是盒子里最常装的四种东西：整数、小数、文字、真假值。\n\n**术语**：变量是对对象的引用（名字），Python 是**动态类型语言**——类型属于值而非变量，在运行时确定且可改变。四种基本类型：`int`（整数）、`float`（浮点数）、`str`（字符串）、`bool`（布尔值，只有 `True`/`False`）。\n\n## 为什么重要\n\n- 变量是编程的基石，一切程序都在对数据做存取和运算。\n- 动态类型灵活但暗藏坑：`\"1\" + 1` 会直接报 `TypeError`，因为字符串和整数不能相加。\n- 类型判断与转换是调试、接口校验、处理用户输入的日常操作，不理解类型寸步难行。\n\n## 核心用法\n\n**1. 赋值即创建，类型随值走**\n`x = 10` 时 x 指向 int；接着 `x = \"hi\"` 完全合法，x 变成了 str。变量本身没有固定类型。\n\n**2. 查看类型：`type()` 与 `isinstance()`**\n- `type(x)`：返回对象的精确类型，常用于调试。\n- `isinstance(x, 类型)`：判断是否为某类型（含父类），可传元组一次判断多种，是生产代码推荐写法。\n\n**3. 类型转换：`int()` / `float()` / `str()` / `bool()`**\n常见坑：\n- `int(\"3.5\")` 报 `ValueError`——int 只认\"长得像整数\"的字符串，需先 `float()` 再 `int()`。\n- `int(3.9)` 得 3——截断取整而非四舍五入，四舍五入用 `round()`（注意它是银行家舍入，`round(2.5) == 2`）。\n- `0.1 + 0.2 != 0.3`——浮点数二进制存储存在精度误差，比较用 `round()` 或 `math.isclose()`。\n- `input()` 永远返回 `str`，参与算术前必须转换。\n- `bool` 是 `int` 的子类：`True == 1`，`True + True == 2`，且空值（`\"\"`、`0`、`None`）转 bool 均为 `False`。\n\n## 代码示例\n\n```Python\n# 1. 动态类型：同一个名字可指向不同类型\nx = 10        # x -> int\nx = \"hello\"   # x -> str，完全合法\n\n# 2. 查看类型\nprint(type(3), type(3.0), type(\"3\"), type(True))\n# <class \'int\'> <class \'float\'> <class \'str\'> <class \'bool\'>\n\na = 5\nprint(isinstance(a, int))          # True\nprint(isinstance(a, (int, str)))   # True，可传元组判断多种类型\nprint(isinstance(True, int))      # True！bool 是 int 的子类\n\n# 3. 基本运算\nprint(7 / 2)    # 3.5，/ 结果必为 float\nprint(7 // 2)   # 3，整除向下取整\nprint(7 % 2)    # 1，取余\n\n# 4. 类型转换的坑\n# 坑1：字符串含小数点不能直接转 int\nprint(int(float(\"3.5\")))   # 3，正确写法\n\n# 坑2：浮点精度\nprint(0.1 + 0.2 == 0.3)         # False\nprint(round(0.1 + 0.2, 1))     # 0.3\n\n# 坑3：int() 是截断不是四舍五入\nprint(int(3.9))   # 3\nprint(round(3.5))  # 4，但 round(2.5) 是 2（银行家舍入）\n\n# 坑4：input() 返回 str，必须转换\n# age = input(\"年龄：\")     # 输入 18，得到 \"18\"\n# age = int(age) + 1        # 转换后才能运算\n\n# 坑5：bool 与 int 打通\nprint(True + True)                  # 2\nprint(bool(\"\"), bool(0), bool(None))  # False False False\n```\n\n## 面试常问\n\n**Q1：Python 是静态还是动态类型？**\n动态类型。变量无需声明类型，类型在运行时由所指向的值决定，且可随时改变（强类型：不同类型不会隐式混算，如 `\"1\" + 1` 报错）。\n\n**Q2：type() 和 isinstance() 有何区别？**\n`type()` 返回精确类型、不考虑继承；`isinstance()` 考虑父子关系（如 `isinstance(True, int)` 为 True）且支持元组参数，判断类型优先用它。\n\n**Q3：为什么 `0.1 + 0.2 == 0.3` 是 False？**\n浮点数按 IEEE 754 二进制存储，0.1 和 0.2 无法精确表示导致误差；应使用 `round()` 或 `math.isclose()` 比较。\n\n**Q4：`int(\"3.5\")` 的结果是什么？**\n抛出 `ValueError`。`int()` 只接受整数字符串，正确写法是 `int(float(\"3.5\"))`。\n\n**Q5：True 和 1 相等吗？**\n相等。bool 是 int 的子类，True == 1、False == 0，所以 `True + True == 2`，也因此用 `type(True) is bool` 才能严格区分二者。','generate://py-var-types','python','generated',0,'2026-09-03 20:40:22','2026-09-03 20:40:22',0,NULL,NULL),(240,'Python 控制流：if / for / while','# Python 控制流：if / for / while\n\n## 是什么\n\n大白话：控制流就是程序的“方向盘和油门”——`if` 负责“看情况走哪条路”，`for` / `while` 负责“一圈圈重复跑”，`break` / `continue` 负责“急刹”和“跳过”。\n\n术语：控制流语句根据条件判断与迭代控制代码块的执行路径，决定哪些代码执行、执行几次。\n\n## 为什么重要\n\n- 没有控制流，程序只能从上到下“傻瓜式”执行，写不出任何有逻辑的功能。\n- Python 靠**缩进**划分代码块，缩进写错会直接报 `IndentationError`，是新手第一大坑。\n- 循环 `else`、`match-case` 是 Python 特色语法，也是面试高频考点。\n\n## 核心用法\n\n**1. 缩进即语法**\nPython 不用大括号 `{}`，而是用缩进（官方推荐 4 个空格）表示“哪些代码属于 if / for / while”。冒号 `:` 引出代码块，同一层级缩进必须完全一致，多一格少一格都报错。\n\n**2. if / elif / else**\n大白话：满足条件就做 A，否则看下一个条件，都不满足就做 C。条件从上到下依次判断，命中一个分支后其余分支不再执行。\n\n**3. for-in 与 range**\n大白话：for 是“挨个拿”——从列表、字符串等可迭代对象中逐个取出元素。`range(start, stop, step)` 生成整数序列，**左闭右开**：包含 start、不包含 stop。\n\n**4. while**\n大白话：条件为真就一直转。循环体内必须让条件有机会变假，否则死循环。适合“事先不知道循环几次”的场景。\n\n**5. break / continue / else**\n- `break`：立刻终止整个循环。\n- `continue`：跳过本次循环剩余代码，直接进入下一轮。\n- 循环 `else`：循环**没有被 break 打断**、正常跑完时才执行，常用于“搜索失败”提示。这是 Python 独有且容易被忽略的语法。\n\n**6. match-case（Python 3.10+）**\n大白话：升级版 switch。除了匹配具体值，还能对序列、字典做“结构化解构”匹配，`case _` 相当于 default。\n\n## 代码示例\n\n```python\n# if / elif / else：注意缩进决定了归属\nscore = 85\nif score >= 90:\n    grade = \"优秀\"        # 缩进 4 格，属于 if\nelif score >= 60:\n    grade = \"及格\"\nelse:\n    grade = \"不及格\"\nprint(grade)               # 不缩进，在 if 之外\n```\n\n```python\n# for-in：挨个取出元素\nfor fruit in [\"苹果\", \"香蕉\", \"橙子\"]:\n    print(fruit)\n\n# range(起点, 终点, 步长)：左闭右开，输出 1 3 5 7 9\nfor i in range(1, 10, 2):\n    print(i)\n```\n\n```python\n# while：条件变假才停\ncount = 3\nwhile count > 0:\n    print(count)\n    count -= 1    # 忘记更新条件就会死循环\n```\n\n```python\n# break / continue / 循环 else\nnums = [1, 3, 5, 8, 9]\nfor n in nums:\n    if n < 3:\n        continue          # 跳过本轮\n    if n % 2 == 0:\n        print(f\"找到偶数 {n}\")\n        break             # 终止整个循环\nelse:\n    print(\"没有找到偶数\")   # 未被 break 打断才执行\n```\n\n```python\n# match-case：结构化模式匹配\ndef handle(cmd: str) -> str:\n    match cmd.split():\n        case [\"go\", direction]:      # 解构出 direction\n            return f\"向 {direction} 移动\"\n        case [\"quit\"]:\n            return \"再见\"\n        case _:                      # 兜底，相当于 default\n            return \"未知命令\"\n\nprint(handle(\"go north\"))  # 向 north 移动\n```\n\n## 面试常问\n\n**Q1：Python 靠什么划分代码块？**\nA：缩进，官方推荐 4 个空格。缩进是语法的一部分而非代码风格，不一致会抛 `IndentationError`。\n\n**Q2：range(1, 10, 2) 会产生哪些数？**\nA：1、3、5、7、9。左闭右开，第三个参数是步长；range 是惰性对象，不会一次性生成整个序列。\n\n**Q3：for / while 的 else 什么时候执行？**\nA：循环正常结束（未被 break 打断）时执行，包括一次都没进循环的情况；一旦被 break 就不执行。\n\n**Q4：break 和 continue 的区别？**\nA：break 结束整个循环；continue 只结束当前这一轮，跳到下一轮迭代。\n\n**Q5：match-case 相比一串 if-elif 有什么优势？**\nA：支持结构化模式匹配，可按字面量、结构（序列/字典）匹配并解构出变量，代码更清晰；需要 Python 3.10 及以上。','generate://py-control-flow','python','generated',0,'2026-09-03 20:41:24','2026-09-03 20:41:24',0,NULL,NULL),(241,'Python 内置容器：列表 / 元组 / 字典 / 集合','## 是什么\n\nPython 内置容器（container）就是\"装数据的盒子”，一个盒子里可以放多个元素。四种最常用的盒子：\n\n- **列表（list）**：像一张购物清单，元素有顺序，可以随时增删改 → 可变、有序。\n- **元组（tuple）**：像封装好的快递，内容写死不能改 → 不可变、有序。\n- **字典**：像通讯录，用\"名字\"（键）查\"电话”（值）→ 键值对，可变，键唯一。\n- **集合**：像数学里的集合，元素不重复，没有顺序 → 可变、元素唯一。\n\n用术语说：它们是 Python 内置的序列或映射类型，分别提供了有序可变、有序不可变、键值映射、无序去重四种数据组织方式。\n\n## 为什么重要\n\n这四个容器覆盖了日常开发 90% 的数据存储需求：\n\n- 保存“一堆东西”用列表（如待办事项、爬取的 URL）；\n- 保存“不该被改动的固定数据\"用元组（如坐标、配置常量）；\n- 保存\"一一对应\"关系用字典（如用户名→密码）；\n- 需要\"去重\"或\"判断某元素存不存在”用集合（如过滤重复 ID）。\n\n选错容器会导致代码又慢又难写：比如用 list 判断 `in` 是 O(n)，用 set 只要 O(1)。这也是面试的高频考点。\n\n## 核心用法\n\n**可变 vs 不可变（核心区别）**：list、dict、set 是可变的，创建后还能修改内容；tuple 创建后不能改。因此 tuple 可以作为 dict 的键，list 不行（不可哈希）。\n\n**列表**：`append` / `pop` / `sort` 增删排序，切片取子段。\n\n**元组**：定义后不可改，常用于函数返回多个值、解包赋值。\n\n**字典**：\n- 取值推荐 `d.get(key, default)`，键不存在时返回默认值而不是报 `KeyError`；\n- 遍历推荐 `for k, v in d.items()`，别再写 `d.keys()` 下标取值了。\n\n**集合**：\n- `set(列表)` 一行去重；\n- 交并差运算：`&`（交集）、`|`（并集）、`-`（差集）；\n- 成员判断 `in` 是 O(1)，比列表快得多。\n\n## 代码示例\n\n```Python\n# 1. 列表：有序、可变\ntodo = [\"买菜\", \"写代码\"]\ntodo.append(\"运动\")           # 末尾添加\ntodo.remove(\"买菜\")           # 删除元素\nprint(todo[0], todo[-1])      # 索引取值，-1 是最后一个\nnums = [3, 1, 2]\nnums.sort()                  # 原地排序\n\n# 2. 元组：有序、不可变\npoint = (3, 4)\nx, y = point                 # 解包赋值\n# point[0] = 10              # 报错！元组不可修改\nd = {point: \"起点\"}           # 元组可哈希，能当字典的键\n\n# 3. 字典：键值对，键唯一\nscore = {\"Alice\": 90, \"Bob\": 85}\nprint(score.get(\"Alice\"))         # 90\nprint(score.get(\"Tom\", 0))        # 键不存在，返回默认值 0，不报错\n# print(score[\"Tom\"])             # 这样写会抛 KeyError\nfor name, s in score.items():      # 推荐的遍历方式\n    print(f\"{name}: {s}\")\nscore[\"Tom\"] = 60                  # 新增/修改\n\n# 4. 集合：去重、集合运算\nids = [1, 2, 2, 3, 3, 3]\nunique_ids = set(ids)              # {1, 2, 3} 自动去重\na, b = {1, 2, 3}, {2, 3, 4}\nprint(a & b)                       # 交集 {2, 3}\nprint(a | b)                       # 并集 {1, 2, 3, 4}\nprint(a - b)                       # 差集 {1}\nprint(2 in a)                      # 成员判断，O(1) 极快\n```\n\n## 面试常问\n\n**Q1：list 和 tuple 有什么区别？**\ntuple 不可变、可哈希、可作 dict 键；list 可变，性能开销略大。固定数据用 tuple，需增删改用 list。\n\n**Q2：dict 中 get 和 `[]` 取值的区别？**\n`d[key]` 键不存在抛 `KeyError`；`d.get(key, default)` 返回默认值（默认 None），更安全。\n\n**Q3：怎么给列表去重？哪种方式保序？**\n`set(lst)` 最快但不保序；`list(dict.fromkeys(lst))` 既去重又保留原始顺序。\n\n**Q4：dict 的键可以是 list 吗？**\n不可以。键必须可哈希（不可变），list、dict、set 都不行；tuple、str、int 可以。\n\n**Q5：为什么判断元素是否存在时用 set 而不是 list？**\nset 基于哈希表，`in` 查找是 O(1)；list 需逐个比较，是 O(n)，数据量大时差距明显。','generate://py-collections','python','generated',0,'2026-09-03 20:42:00','2026-09-03 20:42:00',0,NULL,NULL),(242,'Python 函数：参数 / 作用域 / lambda','# Python 函数：参数 / 作用域 / lambda\n\n## 是什么\n\n大白话：函数就像一台自动贩卖机——你投币（传参数），它掉出饮料（返回结果）。参数是入口；作用域规定“哪些变量在这台机器里能看见”；lambda 是一种一行写完的小函数。\n\n术语：函数是封装了特定逻辑、可重复调用的代码块；参数是调用时的输入接口；作用域是变量的可见范围；lambda 是创建匿名函数的表达式。\n\n## 为什么重要\n\n1. 写一次，到处用：函数是代码复用的最小单元。\n2. 参数设计决定接口灵活度：会不会用 `*args/**kwargs`、会不会踩默认参数的坑，直接体现 Python 功底。\n3. 作用域理解不清，就会出现“变量明明定义了却报 NameError”的经典 bug。\n4. 闭包是装饰器、工厂函数的底层原理，面试必考。\n5. lambda 能写出更简洁的回调式代码（排序、过滤）。\n\n## 核心用法\n\n### 1. 位置参数与关键字参数\n\n- 位置参数：按顺序对应，`f(1, 2)` 中 1 给第一个参数。\n- 关键字参数：按名字对应，`f(x=1, y=2)`，顺序随意、可读性好。\n- 定义时用 `/` 强制其前面的参数只能按位置传；用 `*` 强制其后面的参数只能按关键字传。\n\n### 2. 默认参数的可变对象陷阱（重点）\n\n默认值在**函数定义时只计算一次**，不是每次调用都算。若默认值是列表、字典等可变对象，所有调用会**共享同一个对象**，上一次的修改会“残留”到下一次。正确做法：默认值设为 `None`，在函数体内新建对象。\n\n### 3. *args 与 **kwargs\n\n- `*args`：把多余的**位置参数**打包成元组。\n- `**kwargs`：把多余的**关键字参数**打包成字典。\n- 定义时用于“收集”，调用时用于“解包”（如 `f(*lst, **d)`）。\n\n### 4. 作用域：LEGB 规则\n\nPython 查找变量的顺序：**L**ocal（函数内）→ **E**nclosing（外层函数）→ **G**lobal（模块级）→ **B**uilt-in（内置）。想在函数内改全局变量用 `global`；想改外层函数的变量用 `nonlocal`。\n\n### 5. 闭包\n\n内层函数“记住”了外层函数的变量，且外层函数把这个内层函数返回出去。即使外层函数已执行完毕，这些变量依然活着——这是装饰器的实现基础。\n\n### 6. lambda\n\n语法：`lambda 参数: 表达式`。只能写一个表达式并自动返回其值，适合临时小逻辑，常与 `sorted`、`map`、`filter` 搭配。\n\n## 代码示例\n\n```Python\n# 1. 位置参数与关键字参数\ndef introduce(name, greeting=\"你好\"):\n    print(f\"{greeting}, {name}!\")\n\nintroduce(\"小明\")                      # 位置参数\nintroduce(name=\"小红\", greeting=\"早\")   # 关键字参数，顺序随意\n\n# 2. 可变默认参数陷阱\ndef add_item_bad(item, lst=[]):        # 错误示范\n    lst.append(item)\n    return lst\n\nprint(add_item_bad(1))    # [1]\nprint(add_item_bad(2))    # [1, 2] ← 上一次的 1 残留了！\n\ndef add_item_good(item, lst=None):     # 正确写法\n    if lst is None:\n        lst = []\n    lst.append(item)\n    return lst\n\n# 3. *args / **kwargs\ndef summary(*args, **kwargs):\n    print(\"位置参数:\", args)       # 元组\n    print(\"关键字参数:\", kwargs)   # 字典\n\nsummary(1, 2, 3, mode=\"fast\", debug=True)\n\n# 4. 闭包 + 作用域\ndef make_counter():\n    count = 0                # 外层函数的变量\n    def counter():\n        nonlocal count       # 修改外层变量必须声明 nonlocal\n        count += 1\n        return count\n    return counter           # 返回内层函数，count 被它\"记住\"\n\nc = make_counter()\nprint(c(), c(), c())         # 1 2 3\n\n# 5. lambda 常用场景：按规则排序\nstudents = [(\"小明\", 88), (\"小红\", 95), (\"小刚\", 72)]\nstudents.sort(key=lambda s: s[1], reverse=True)\nprint(students)              # 按分数从高到低\n```\n\n## 面试常问\n\n**Q1：为什么默认参数不能写成 `def f(x, lst=[])`？**\n默认值只在定义时求值一次，列表被所有调用共享，导致上次的修改残留。应默认为 `None` 并在函数体内新建。\n\n**Q2：`*args` 和 `**kwargs` 的区别？**\n`*args` 收集多余位置参数为元组；`**kwargs` 收集多余关键字参数为字典。\n\n**Q3：简述 LEGB。**\nPython 查找变量的顺序：局部 → 外层函数（闭包）→ 全局 → 内置。\n\n**Q4：什么是闭包？**\n内层函数引用了外层函数的变量并被返回，从而“记住”定义时的环境；是装饰器的实现基础。\n\n**Q5：lambda 和 def 有何区别？**\nlambda 只能写单个表达式、无函数名，适合简单临时逻辑；def 可写完整逻辑、可命名复用。','generate://py-functions','python','generated',0,'2026-09-03 20:42:55','2026-09-03 20:42:55',0,NULL,NULL),(243,'Python 字符串常用操作','# Python 字符串常用操作\n\n## 是什么\n\n大白话讲，字符串就是“一段文字”。在 Python 里，凡是用引号（单引号或双引号都行）包起来的内容就是字符串，比如 `\"hello\"`。你可以把它想象成一串珠子：每个字符是一颗珠子，珠子有编号（从 0 开始），所以你能数出总共有几颗、取出其中几颗，也能把几串珠子拼在一起。\n\n术语上，字符串是 Python 的内置类型 `str`，用于存储 Unicode 字符序列，属于**不可变**的序列类型。\n\n## 为什么重要\n\n- 文本处理无处不在：日志分析、数据清洗、爬虫、Web 开发、AI 提示词处理，都离不开字符串操作。\n- 它是初学者接触的第一个“序列类型”，学会字符串切片，后面学列表、元组的切片就水到渠成。\n- 掌握 `split`/`join`/`strip` 等方法能少写很多循环，代码更简洁、更 Pythonic。\n- 它是面试高频考点：不可变性、f-string、切片规则几乎必考。\n\n## 核心用法\n\n**1. 切片：`s[start:stop:step]`**\n规则是“含头不含尾”：取到 stop 前一位就停。start、stop、step 都可以省略；负数表示从右往左数。`s[::-1]` 是反转字符串的经典写法。\n\n**2. 常用方法**\n- `split(sep)`：按分隔符把字符串切成列表，像切西瓜；\n- `join(iterable)`：用指定字符串把列表拼起来，像串珠子，与 split 互为逆操作；\n- `strip()`：去掉首尾的空白（或指定字符），`lstrip`/`rstrip` 分别只去左边/右边，常用于清洗用户输入；\n- `format()`：占位符填空，如 `\"{}今年{}岁\".format(name, age)`；\n- 另外常用的还有 `upper`/`lower`/`replace`/`find`/`count`/`startswith`/`endswith`。\n\n**3. f-string（格式化字符串字面量）**\n在字符串前加 `f`，变量直接写进花括号：`f\"{name}今年{age}岁\"`。花括号里还能写表达式和格式说明符，如 `f\"{pi:.2f}\"` 表示保留两位小数。它是 Python 3.6+ 的推荐写法，简洁且性能最好。\n\n**4. 不可变性**\n字符串一旦创建，内容不能被原地修改。任何“修改”操作（`replace`、`upper` 等）都是**返回一个新字符串**，原字符串纹丝不动。所以 `s[0] = \'a\'` 会直接报错。\n\n## 代码示例\n\n```Python\n# 1. 切片\ns = \"Python很强大\"\nprint(s[0])       # \'P\'，第一个字符，下标从 0 开始\nprint(s[0:6])     # \'Python\'，含头不含尾\nprint(s[-3:])     # \'很强大\'，负数从右往左数\nprint(s[::-1])    # \'大强很nohtyP\'，反转字符串\n\n# 2. split / join：字符串与列表互转\ntext = \"apple,banana,cherry\"\nfruits = text.split(\",\")      # [\'apple\', \'banana\', \'cherry\']\nprint(\"-\".join(fruits))       # \'apple-banana-cherry\'\n\n# 3. strip：清洗输入的首尾空白\nname = \"  张三  \"\nprint(name.strip())           # \'张三\'\n\n# 4. format 与 f-string\nname, age, pi = \"李四\", 18, 3.14159\nprint(\"你好，{}，你{}岁了\".format(name, age))  # 你好，李四，你18岁了\nprint(f\"你好，{name}，你{age}岁了\")            # 推荐写法\nprint(f\"圆周率约为{pi:.2f}\")                   # 圆周率约为3.14\n\n# 5. 不可变性\ns = \"hello\"\ntry:\n    s[0] = \"H\"                # 试图原地修改\nexcept TypeError as e:\n    print(e)                  # \'str\' object does not support item assignment\ns2 = \"H\" + s[1:]              # 正确做法：生成新字符串\nprint(s, s2)                  # hello Hello\n```\n\n## 面试常问\n\n**Q1：字符串是可变的吗？**\nA：不可变。任何修改都返回新对象，原字符串不变；这使它能被安全地用作字典的键，也能在多线程间共享。\n\n**Q2：如何反转一个字符串？**\nA：`s[::-1]`，利用切片的负步长一步完成，无需循环。\n\n**Q3：split 和 join 是什么关系？**\nA：互为逆操作——split 把字符串拆成列表，join 把列表拼成字符串；注意调用方式是 `\"分隔符\".join(列表)`，而不是 `列表.join()`。\n\n**Q4：为什么推荐 f-string 而不是 format 或 %？**\nA：f-string 语法最简洁、可读性最好，运行性能也最优（Python 3.6+ 首选）。\n\n**Q5：如何判断子串是否存在、统计出现次数？**\nA：`\"a\" in s` 返回布尔值判断包含；`s.count(\"a\")` 统计次数，`s.find(\"a\")` 返回首次出现的下标（找不到返回 -1）。','generate://py-strings','python','generated',0,'2026-09-03 20:43:37','2026-09-03 20:43:37',0,NULL,NULL),(244,'Python 类与面向对象基础','# Python 类与面向对象基础\n\n## 是什么\n\n大白话：类就是一张“图纸”或“模具”，对象就是照着图纸造出来的“产品”。比如“狗”是一个类，它描述了所有狗的共同点（有名字、会叫）；而你家那只叫“旺财”的狗，就是一个具体的对象（实例）。\n\n术语：类是把数据（属性）和操作数据的行为（方法）封装在一起的模板；面向对象编程（OOP）就是围绕“类与对象”来组织代码的编程思想，核心特征是封装、继承、多态。\n\n## 为什么重要\n\n1. Python 中一切皆对象——字符串、列表、函数都是对象，不懂类就看不懂 Python 的底层逻辑。\n2. 封装让代码更整洁：相关的数据和函数打包在一起，而不是散落一地。\n3. 继承支持复用与扩展，避免重复造轮子。\n4. 主流框架大量基于类：Django 的视图、Pytest 的测试类、pandas 的 DataFrame 都离不开类，看不懂类就看不懂框架源码。\n\n## 核心用法\n\n**1. 定义类与 __init__**\n用 `class 类名:` 定义类；`__init__` 是构造方法，创建对象时自动执行，用于初始化属性；`self` 代表“当前这个对象本身”，是实例方法的第一个参数。\n\n**2. 实例属性 vs 类属性**\n- 类属性：写在类体中，所有实例共享一份；\n- 实例属性：通过 `self.xxx` 绑定，每个对象各自独立；\n- 查找顺序：先找实例属性，找不到再找类属性。\n\n**3. 继承**\n`class Dog(Animal)` 表示 Dog 继承 Animal，自动获得父类的属性和方法；`super()` 用于调用父类方法；子类定义同名方法即为“重写”。\n\n**4. 魔法方法**\n以双下划线包围的特殊方法（如 `__init__`、`__str__`、`__eq__`、`__len__`），不需要手动调用，Python 会在特定时机（如 print、== 比较）自动触发，是定制对象行为的关键。\n\n## 代码示例\n\n```python\nclass Animal:\n    species = \"动物\"  # 类属性：所有实例共享\n\n    def __init__(self, name, age):\n        self.name = name  # 实例属性：每个对象各自一份\n        self.age = age\n\n    def speak(self):\n        return f\"{self.name} 会发出声音\"\n\n\nclass Dog(Animal):\n    \"\"\"Dog 继承自 Animal\"\"\"\n\n    def __init__(self, name, age, breed):\n        super().__init__(name, age)  # 调用父类构造方法\n        self.breed = breed\n\n    def speak(self):  # 方法重写\n        return f\"{self.name} 汪汪叫\"\n\n    def __str__(self):  # print 时自动触发\n        return f\"Dog({self.name}, {self.age}岁, {self.breed})\"\n\n    def __eq__(self, other):  # == 比较时自动触发\n        return self.name == other.name\n\n\ndog1 = Dog(\"旺财\", 3, \"柴犬\")\ndog2 = Dog(\"旺财\", 5, \"柯基\")\n\nprint(dog1.speak())   # 旺财 汪汪叫\nprint(dog1)            # Dog(旺财, 3岁, 柴犬) —— 触发 __str__\nprint(dog1 == dog2)    # True —— 触发 __eq__\nprint(Dog.species)     # 动物 —— 类属性可由类或实例访问\nprint(isinstance(dog1, Animal))  # True —— 子类实例也是父类类型\n```\n\n## 面试常问\n\n**Q1：`__init__` 和 `__new__` 的区别？**\n`__new__` 负责创建实例（先调用、返回对象），`__init__` 负责初始化实例（后调用、不返回值）；日常主要写 `__init__`。\n\n**Q2：实例属性和类属性的区别？**\n类属性所有实例共享，实例属性各自独立；查找时先实例后类。注意：通过实例对类属性赋值会在实例上新建同名属性“遮蔽”它，并不修改类属性本身。\n\n**Q3：self 是什么？必须写吗？**\nself 指向当前实例，是实例方法的第一个参数，名字可换但约定俗成；调用时 Python 自动传入，无需手动传。\n\n**Q4：`__str__` 和 `__repr__` 的区别？**\n`__str__` 面向用户，print/str() 时触发；`__repr__` 面向开发者，解释器直接显示或 repr() 触发；未定义 `__str__` 时会回退使用 `__repr__`。\n\n**Q5：子类重写方法后如何复用父类逻辑？**\n用 `super().方法名()` 调用，典型场景是子类 `__init__` 中先执行父类的初始化再补充自己的属性。','generate://py-oop','python','generated',0,'2026-09-03 20:44:12','2026-09-03 20:44:12',0,NULL,NULL),(245,'Python 模块 / 包 / 虚拟环境','# Python 模块 / 包 / 虚拟环境\n\n## 是什么\n\n大白话：**模块**就是一个 `.py` 文件；**包**是一个装着多个模块的文件夹（里面有 `__init__.py`）；**虚拟环境**是给某个项目单独配的一套 Python 解释器 + 第三方库，相当于“项目专属工具箱”。\n\n术语化：模块是 Python 的基本代码组织单元；包是按目录层级组织的模块集合；虚拟环境是一个隔离的运行时，拥有独立的 `site-packages`（第三方库存放目录）。\n\n## 为什么重要\n\n1. **复用与分层**：把工具函数拆成模块，避免单个文件几千行，也方便跨项目复用。\n2. **依赖隔离**：项目 A 要 `requests 2.x`，项目 B 要 `3.x`，各装在各自虚拟环境里互不打架，也不污染全局。\n3. **生态基础**：Python 的强大一半靠标准库，一半靠 pip 上百万个第三方包，而用好它们的前提就是理解模块 / 包 / 环境。\n\n## 核心用法\n\n### import 机制\n\n- **查找顺序**：执行 `import` 时，Python 按 `sys.path` 列表依次查找——脚本所在目录 → `PYTHONPATH` → 标准库 → `site-packages`。\n- **缓存**：模块首次导入时执行一次，结果缓存进 `sys.modules`，之后再 import 直接复用，不会重复执行。\n- **三种写法**：`import os`（用 `os.path`）、`from os import path`（直接用 `path`）、`import numpy as np`（起别名）。\n- **循环导入是大坑**：两个模块在顶层互相 import 会报 `ImportError`，解法是把 import 移到函数内，或重构模块拆分。\n\n### `__name__ == \'__main__\'`\n\n每个模块都有 `__name__` 属性：直接用 `python xx.py` 运行时它等于 `\'__main__\'`；被别人 import 时等于模块名。所以 `if __name__ == \'__main__\':` 下的代码只在“亲自运行”时执行——把测试代码和程序入口放这里最合适。\n\n### pip 与 venv / uv\n\n- **venv**：`python -m venv .venv` 创建环境，激活后 `python` 和 `pip` 都指向这个环境。\n- **pip**：`pip install requests` 装包，`pip freeze > requirements.txt` 导出依赖清单，`pip install -r requirements.txt` 一键还原。\n- **uv**：新一代工具，速度快得多：`uv venv` 建环境，`uv pip install` 兼容 pip 用法；`uv add requests` 则写入 `pyproject.toml`，是更现代的管理方式。\n\n## 代码示例\n\n```Python\n# mymath.py —— 一个模块\ndef add(a, b):\n    return a + b\n\n# 只在直接运行本文件时执行，被 import 时不执行\nif __name__ == \'__main__\':\n    print(add(1, 2))     # 终端 python mymath.py → 输出 3\n```\n\n```Python\n# main.py —— 使用上面的模块\nimport mymath              # 导入整个模块\nfrom mymath import add     # 只导入某个函数\n\nprint(mymath.add(10, 5))   # 15\nprint(add(3, 4))            # 7\n```\n\n```Python\n# 包结构示例：\n# mypkg/\n#   __init__.py\n#   utils/\n#       __init__.py\n#       text.py        ->  def clean(s): return s.strip()\n\nfrom mypkg.utils.text import clean   # 绝对导入：点号表示层级\nfrom . import text                    # 相对导入：只能在包内部用\n\n# 观察 import 的查找路径和缓存\nimport sys\nprint(sys.path[:3])                   # 模块搜索路径\nprint(\'mymath\' in sys.modules)        # True，说明已缓存\n```\n\n```Python\n# 命令行操作（venv + pip）\n# python -m venv .venv                 # 创建虚拟环境\n# source .venv/bin/activate            # macOS / Linux 激活\n# .venv\\Scripts\\activate               # Windows 激活\n# pip install requests                 # 安装第三方库\n# pip freeze > requirements.txt         # 导出依赖清单\n# pip install -r requirements.txt      # 一键还原环境\n\n# uv 方式（更快）\n# uv venv && source .venv/bin/activate\n# uv pip install requests\n# uv add requests                      # 写入 pyproject.toml\n```\n\n## 面试常问\n\n1. **Q：`if __name__ == \'__main__\':` 的作用？**\n   A：保证代码块只在直接运行该文件时执行，被 import 时不执行，常用于测试代码和程序入口。\n\n2. **Q：import 一个模块时发生了什么？**\n   A：按 `sys.path` 查找 → 执行模块顶层代码一次 → 缓存进 `sys.modules`，之后再 import 直接复用缓存。\n\n3. **Q：为什么要用虚拟环境？**\n   A：隔离各项目的第三方依赖，避免版本冲突，同时不污染全局解释器。\n\n4. **Q：模块和包的区别？**\n   A：模块是单个 `.py` 文件；包是含 `__init__.py` 的目录，可嵌套组织多个模块。\n\n5. **Q：pip 和 uv 的区别？**\n   A：pip 是官方标准包管理器；uv 是 Rust 编写的现代替代品，兼容 pip 用法但速度快十倍以上，还支持项目管理（`uv add` + `pyproject.toml`）。','generate://py-modules','python','generated',0,'2026-09-03 20:45:16','2026-09-03 20:45:16',0,NULL,NULL),(246,'Python 异常处理','# Python 异常处理\n\n## 是什么\n\n大白话：程序运行时难免遇到“意外”——除以零、打开不存在的文件、用了没定义的变量。这些意外在 Python 里叫**异常**。异常处理就是提前安排好“出了意外怎么办”，让程序不崩溃、能继续跑下去。\n\n术语版：异常本质是一个对象。当 Python 无法正常执行某段代码时，会**抛出**一个异常对象；若无人**捕获**处理，程序终止并打印 traceback。`try/except` 语句就是捕获和处理异常的标准机制。\n\n## 为什么重要\n\n1. **防止程序崩溃**：用户输错一个数，不代表整个系统该挂掉。\n2. **分离正常逻辑与错误处理**：主流程写 try 里，出错怎么办写 except 里，代码更清晰。\n3. **生产级代码的标配**：文件读写、网络请求、数据库操作，哪一样都可能失败，不做异常处理等于裸奔。\n4. **Python 官方推荐风格（EAFP）的基础**，见后文。\n\n## 核心用法\n\n### 1. try/except/else/finally 四件套\n\n- `try`：放可能出错的代码。\n- `except`：出错后执行，可指定捕获哪种异常，`except XxxError as e` 能拿到异常对象。\n- `else`：**没出错**才执行（注意：不是“否则”）。\n- `finally`：**无论如何都执行**，常用于关闭文件、释放资源。\n\n### 2. 常见异常类型\n\n| 异常 | 触发场景 |\n|---|---|\n| `ZeroDivisionError` | 除以零 |\n| `ValueError` | 类型对但值不合法，如 `int(\"abc\")` |\n| `TypeError` | 类型不对，如 `\"a\" + 1` |\n| `KeyError` / `IndexError` | 字典键不存在 / 序列下标越界 |\n| `FileNotFoundError` | 打开的文件不存在 |\n| `AttributeError` | 对象没有该属性或方法 |\n| `NameError` | 使用未定义的变量 |\n\n### 3. raise：主动抛出异常\n\n`raise` 用于手动制造异常，典型场景是参数校验、把底层错误翻译成业务错误；在 except 块里裸写 `raise` 可把当前异常原样向上抛。\n\n### 4. 自定义异常\n\n定义一个继承 `Exception` 的类即可。好处：异常名即业务语义，调用方能精准捕获你定义的错误，而不是靠猜内置异常。\n\n### 5. EAFP 风格\n\nEAFP = *Easier to Ask Forgiveness than Permission*（先干了再说，出事再道歉），反面是 LBYL（*Look Before You Leap*，三思而后行）。Python 社区推崇 EAFP：直接尝试操作、用异常兜底，而不是先做一堆检查。原因：检查与实际操作之间可能存在竞态，且异常写法更简洁。\n\n## 代码示例\n\n```Python\n# 1. try/except/else/finally 完整流程\ndef read_score(path):\n    try:\n        score = int(open(path).read())   # 可能 FileNotFoundError / ValueError\n    except FileNotFoundError:\n        print(\"文件不存在，使用默认分 0\")\n        return 0\n    except ValueError as e:              # as e 拿到异常对象\n        print(f\"内容不是数字: {e}\")\n        return 0\n    else:\n        print(\"读取成功\")                 # 没异常才执行\n        return score\n    finally:\n        print(\"收尾工作\")                 # 一定执行（实际项目建议用 with 管理文件）\n\n# 2. raise：主动抛出\ndef set_age(age):\n    if age < 0:\n        raise ValueError(f\"年龄不能为负数: {age}\")\n    return age\n\n# 3. 自定义异常\nclass BalanceError(Exception):\n    \"\"\"余额不足异常\"\"\"\n\ndef withdraw(balance, amount):\n    if amount > balance:\n        raise BalanceError(f\"余额 {balance}，试图取 {amount}\")\n    return balance - amount\n\ntry:\n    withdraw(100, 500)\nexcept BalanceError as e:\n    print(f\"取款失败: {e}\")\n\n# 4. EAFP vs LBYL\n# LBYL：先检查再操作（啰嗦，检查和操作之间数据还可能变化）\nname = user_dict[\"name\"] if \"name\" in user_dict else \"匿名\"\n\n# EAFP：直接干，出错兜底（更 Pythonic）\ntry:\n    name = user_dict[\"name\"]\nexcept KeyError:\n    name = \"匿名\"\n```\n\n## 面试常问\n\n**Q1：`except Exception` 和 `except BaseException` 的区别？**\n`BaseException` 是所有异常的基类，会连 `KeyboardInterrupt`（Ctrl+C）、`SystemExit` 一起捕获，导致程序无法正常中断退出；日常应捕获 `Exception`。\n\n**Q2：finally 什么时候执行？try 里有 return 还执行吗？**\n一定执行，即使 try/except 中出现 return 或再次抛异常；finally 里写 return 会覆盖原返回值，应避免。\n\n**Q3：什么是 EAFP？举例说明。**\n\"Easier to Ask Forgiveness than Permission\"——先执行操作、用 except 兜底，如直接 `d[key]` 并捕获 `KeyError`，而非先用 `in` 检查。\n\n**Q4：如何自定义异常？为什么要自定义？**\n继承 `Exception` 定义类即可。目的是让异常携带业务语义，调用方可 `except MyError` 精准处理，不与内置异常混淆。\n\n**Q5：else 块什么时候执行？**\ntry 中没有发生任何异常时执行，适合放“依赖 try 成功”的后续代码，避免正常逻辑被误捕获。','generate://py-exceptions','python','generated',0,'2026-09-03 20:46:17','2026-09-03 20:46:17',0,NULL,NULL),(247,'Python 文件读写','# Python 文件读写\n\n## 是什么\n\n大白话：程序里的数据（变量）都住在内存里，程序一关就全没了。文件读写就是让数据能在硬盘上“存下来”和“拿回来”——写文件是把内存数据保存到硬盘，读文件是把硬盘内容加载进内存。\n\n术语上，Python 用内置函数 `open()` 打开一个文件对象（也叫文件句柄），再调用它的 `read()`、`write()` 等方法完成读写，最后 `close()` 释放资源。\n\n## 为什么重要\n\n1. **数据持久化**：日志、配置、爬虫结果、分析报表，最终都要落到文件里才不会丢。\n2. **数据处理第一步**：绝大多数任务的起点都是“把文件读进来”，读不对，后面全白搭。\n3. **内存安全**：几 GB 的日志一次性读进内存会撑爆程序，“按需读取”是必备技能。\n4. **中文环境的坑**：乱码问题几乎人人踩过，理解编码能省下大量排查时间。\n\n## 核心用法\n\n### 1. open 与 with\n\n`open(路径, 模式)` 返回文件对象，用完必须 `close()`，否则可能丢数据、占用系统资源。更好的写法是 `with open(...) as f:`：它是上下文管理器，代码块结束时自动关闭文件，**即使中途抛异常也会关闭**，推荐永远用 with。\n\n常用模式：\n\n- `r`：只读（默认，文件不存在报错）\n- `w`：写入（不存在则创建，存在则**清空重写**）\n- `a`：追加（在文件末尾写，不清空原内容）\n- 加 `+` 变读写模式，如 `r+`\n\n### 2. 文本模式 vs 二进制模式\n\n默认是文本模式，读出来是 `str`；加 `b`（如 `rb`、`wb`）是二进制模式，读出来是 `bytes`。\n\n- **文本模式**：适合 .txt、.csv、.json 等，需要指定 `encoding`，Windows 下还会自动把 `\\r\\n` 转成 `\\n`\n- **二进制模式**：适合图片、音视频、压缩包等，按原始字节原样读写，不涉及编码\n\n### 3. 逐行迭代\n\n读大文件的标准姿势是直接 `for line in f:`，内存里每次只有一行，几 GB 也不怕。对比：`f.read()` 一次读全部、`f.readlines()` 一次读成列表，都吃内存，只适合小文件。\n\n### 4. 编码问题\n\n编码就是“字符和字节”的映射规则。最常见的坑：Windows 下 `open()` 不指定 `encoding` 时默认用 GBK，而文件实际是 UTF-8，读出来就是乱码（或抛 `UnicodeDecodeError`）。解法：**永远显式写 `encoding=\'utf-8\'`**，并保证读写两端用同一编码。\n\n## 代码示例\n\n```Python\n# 1. 写文件：with 自动关闭；w 模式会清空原内容\nwith open(\'demo.txt\', \'w\', encoding=\'utf-8\') as f:\n    f.write(\'第一行\\n\')\n    f.writelines([\'第二行\\n\', \'第三行\\n\'])  # writelines 不会自动加换行符\n\n# 2. 逐行迭代读取：大文件友好，内存中每次只保留一行\nwith open(\'demo.txt\', \'r\', encoding=\'utf-8\') as f:\n    for line in f:\n        print(line.rstrip(\'\\n\'))  # 去掉行尾换行符再处理\n\n# 3. 追加写入：a 模式不清空原内容\nwith open(\'demo.txt\', \'a\', encoding=\'utf-8\') as f:\n    f.write(\'追加的一行\\n\')\n\n# 4. 二进制模式：复制图片，原样按字节读写，不涉及编码\nwith open(\'logo.png\', \'rb\') as src, open(\'logo_copy.png\', \'wb\') as dst:\n    while True:\n        chunk = src.read(1024)  # 每次读 1KB，避免大文件占内存\n        if not chunk:\n            break\n        dst.write(chunk)\n```\n\n## 面试常问\n\n**Q1：`with open` 和手动 `open` + `close` 有什么区别？**\nwith 是上下文管理器，离开代码块自动 `close()`，即使发生异常也能保证关闭，避免句柄泄漏和数据丢失。\n\n**Q2：`r`、`w`、`a` 三种模式的区别？**\n`r` 只读且文件必须存在；`w` 清空重写（不存在则创建）；`a` 在末尾追加，不清空。\n\n**Q3：如何读取几 GB 的大文件？**\n用 `for line in f:` 逐行迭代或 `f.read(size)` 分块读取，避免 `read()` / `readlines()` 一次性加载全部内容。\n\n**Q4：读写中文出现乱码怎么排查？**\n本质是读写两端编码不一致。显式指定 `encoding=\'utf-8\'`，并确认文件真实编码（Windows 默认 GBK 是常见根源）。\n\n**Q5：文本模式和二进制模式分别返回什么类型？各适用什么场景？**\n文本模式返回 `str`，适用 .txt/.csv/.json 等文本文件；二进制模式返回 `bytes`，适用图片、音视频等，按原始字节处理。','generate://py-file-io','python','generated',0,'2026-09-03 20:47:18','2026-09-03 20:47:18',0,NULL,NULL),(248,'Python 进阶基础：迭代器 / 生成器 / 装饰器','# Python 进阶基础：迭代器 / 生成器 / 装饰器\n\n## 是什么\n\n**先说大白话：**\n\n- **迭代器**：像一个发牌员，你喊一次，它给一张牌，发完就摊手说“没了”。术语上，它是同时实现了 `__iter__` 和 `__next__` 两个方法的对象。\n- **可迭代对象**：像“一副牌”，自己不直接发牌，但能交给发牌员去发。术语上，它是实现了 `__iter__`、能返回一个迭代器的对象，如 list、str、dict。for 循环的底层逻辑就是：先 `iter()` 拿到迭代器，再反复 `next()`，直到抛出 `StopIteration` 为止。\n- **生成器**：写迭代器的“偷懒写法”。函数里只要出现 `yield`，它就成了生成器函数；调用它不会执行任何代码，而是返回一个生成器对象（生成器本身就是一种迭代器）。每次 `next()` 才执行到下一个 `yield` 处暂停，下次从暂停处继续——这就是**惰性求值**：值在被需要时才计算，算多少取多少，不预先占内存。\n- **装饰器**：给函数“套一层壳”，在不改原函数代码的前提下附加功能（计时、日志、缓存、鉴权）。**本质是高阶函数**：接收一个函数，返回一个新函数；`@deco` 只是 `func = deco(func)` 的语法糖。\n\n## 为什么重要\n\n1. **省内存**：处理大文件、大日志、海量数据流时，生成器按需逐个产出，不必一次性装进内存，避免 OOM。\n2. **理解 Python 遍历的统一机制**：for 循环、解包、推导式、`sum()/max()` 全部建立在可迭代协议之上，懂它才算懂 Python 遍历的底层逻辑。\n3. **代码解耦复用**：装饰器把日志、重试、缓存这类“横切逻辑”从业务代码中剥离出去，Flask/FastAPI 的路由注册、`@property`、`@staticmethod` 底层全是它。\n\n## 核心用法\n\n- **协议三件套**：`iter(obj)` 拿到迭代器 → `next(it)` 取下一个 → 耗尽时抛 `StopIteration`。\n- **生成器**：函数体内 `yield` 值；或用生成器表达式 `(x*x for x in data)`（注意是圆括号，方括号就变成列表了）。\n- **装饰器**：高阶函数 + 闭包捕获原函数；内层 `wrapper` 务必用 `functools.wraps(func)` 装饰，把原函数的元信息（`__name__`、`__doc__` 等）复制过来。\n\n## 代码示例\n\n```Python\n# ---------- 1. 迭代器协议 ----------\nclass CountDown:\n    \"\"\"自己造一个迭代器：从 n 倒数到 1\"\"\"\n    def __init__(self, n):\n        self.n = n\n\n    def __iter__(self):          # 返回自身，说明自己就是迭代器\n        return self\n\n    def __next__(self):\n        if self.n <= 0:\n            raise StopIteration   # 耗尽信号，for 循环靠它停止\n        self.n -= 1\n        return self.n + 1\n\nfor x in CountDown(3):\n    print(x)                      # 3 2 1\n\n\n# ---------- 2. 生成器：惰性求值 ----------\ndef read_lines(path):\n    with open(path, encoding=\"utf-8\") as f:\n        for line in f:            # 逐行产出，整个文件不会进内存\n            yield line.strip()\n\nsquares = (x * x for x in range(10))   # 生成器表达式\nprint(next(squares))              # 0，此刻才真正计算\n\n\n# ---------- 3. 装饰器：高阶函数 + functools.wraps ----------\nimport functools\nimport time\n\ndef timer(func):\n    @functools.wraps(func)        # 关键：保留原函数元信息\n    def wrapper(*args, **kwargs):\n        start = time.time()\n        result = func(*args, **kwargs)\n        print(f\"{func.__name__} 耗时 {time.time() - start:.3f}s\")\n        return result\n    return wrapper\n\n@timer                            # 等价于 slow_add = timer(slow_add)\ndef slow_add(a, b):\n    \"\"\"两数相加\"\"\"\n    time.sleep(0.5)\n    return a + b\n\nslow_add(1, 2)\nprint(slow_add.__name__)          # slow_add；没有 wraps 会打印 wrapper\n```\n\n## 面试常问\n\n1. **可迭代对象和迭代器的区别？** 前者只实现 `__iter__`，后者额外实现 `__next__`；迭代器一定可迭代，而 list 可迭代却不是迭代器（`next([1,2])` 会报错）。\n2. **yield 和 return 的区别？** `return` 终止函数并返回；`yield` 暂停函数交出一个值，保留状态可恢复执行；含 `yield` 的函数被调用后得到的是生成器对象而非结果。\n3. **生成器适合什么场景？** 大文件逐行读取、无限序列、管道式数据处理，核心优势是惰性求值、省内存。\n4. **装饰器的本质是什么？** 一个“接收函数、返回新函数”的高阶函数，`@` 只是语法糖；带参数的装饰器则要再往外多包一层返回装饰器的函数。\n5. **functools.wraps 有什么用？** 把原函数的 `__name__`、`__doc__`、签名等元信息复制到 wrapper 上，否则日志打印、调试、依赖函数名的框架（如序列化、路由注册）会出错。','generate://py-iter-deco','python','generated',0,'2026-09-03 20:48:32','2026-09-03 20:48:32',0,NULL,NULL),(249,'Java 基本语法与数据类型','# Java 基本语法与数据类型\n\n## 是什么\n\n大白话说：变量就像一个贴了标签的盒子，数据类型决定这个盒子多大、能装什么。Java 基本语法就是写代码必须遵守的“交通规则”，比如每句话以分号结尾、代码放在类和方法里。\n\nJava 的数据类型分两大类：\n\n- **基本类型**：8 种，盒子里直接装“值”本身，存放在栈上，速度快。\n- **引用类型**：如 String、数组、对象，盒子里装的是“地址”，指向堆里的真实数据。\n\n八大基本类型：\n\n| 分类 | 类型 | 大小 | 备注 |\n|---|---|---|---|\n| 整数 | byte | 1 字节 | -128 ~ 127 |\n| 整数 | short | 2 字节 | |\n| 整数 | int | 4 字节 | 整数默认类型 |\n| 整数 | long | 8 字节 | 赋值要加 L |\n| 浮点 | float | 4 字节 | 赋值要加 F |\n| 浮点 | double | 8 字节 | 小数默认类型 |\n| 字符 | char | 2 字节 | 单引号，如 \'A\' |\n| 布尔 | boolean | — | 只有 true / false |\n\n## 为什么重要\n\n- 这是所有代码的地基：循环、方法、面向对象都建立在变量和类型之上。\n- 类型选错会导致精度丢失或内存浪费，面试笔试也常考。\n- 包装类装箱、String 不可变是面试必考题，不理解容易写出 Bug。\n- main 方法是程序的唯一入口，写错一个字符程序就跑不起来。\n\n## 核心用法\n\n1. **变量声明**：`类型 变量名 = 值;`，变量名区分大小写。\n2. **类型转换**：小类型→大类型自动转换（安全）；大→小必须强制转换，可能丢失精度。\n3. **包装类与自动装箱**：每个基本类型都有对应的包装类（int→Integer、double→Double），用于泛型、可为 null 等场景。自动装箱/拆箱让基本类型和包装类自动互转。\n4. **var**：Java 10 引入，编译器根据右侧的值推断类型。只能用于局部变量，不能用于成员变量、方法参数，且声明时必须初始化。\n5. **String 不可变**：String 对象一旦创建，内容永远不能改，任何“修改”操作其实都是创建新对象。\n6. **main 方法**：固定签名 `public static void main(String[] args)`，public（外部可访问）、static（不用 new 对象就能调）、void（无返回值）。\n\n## 代码示例\n\n```Java\npublic class BasicDemo {\n    // 程序入口：固定写法，JVM 从这里开始执行\n    public static void main(String[] args) {\n        // 1. 八大基本类型\n        byte b = 100;\n        short s = 20000;\n        int i = 1000000;\n        long l = 10000000000L;   // 注意后缀 L\n        float f = 3.14F;         // 注意后缀 F\n        double d = 3.1415926;\n        char c = \'A\';            // 单引号，单个字符\n        boolean flag = true;\n\n        // 2. 类型转换\n        long wide = i;           // 小转大，自动提升\n        int big = 130;\n        byte small = (byte) big; // 强转，130 溢出变成 -126\n\n        // 3. 包装类与自动装箱/拆箱\n        Integer boxed = i;       // 自动装箱：int -> Integer\n        int unboxed = boxed + 1; // 自动拆箱：Integer -> int\n        Integer x = 127, y = 127;\n        Integer m = 128, n = 128;\n        System.out.println(x == y); // true，缓存池 [-128,127]\n        System.out.println(m == n); // false，超出缓存范围\n\n        // 4. var 类型推断（Java 10+）\n        var name = \"Java\";       // 推断为 String\n        var count = 10;          // 推断为 int\n\n        // 5. String 不可变\n        String str = \"hello\";\n        str.concat(\" world\");    // 原字符串没有变\n        System.out.println(str); // hello\n        String str2 = str + \" world\"; // 创建了新对象\n        System.out.println(str2);     // hello world\n    }\n}\n```\n\n## 面试常问\n\n**1. 八大基本类型分别是什么？**\nbyte、short、int、long、float、double、char、boolean；整数默认 int，小数默认 double。\n\n**2. Integer == Integer 什么时候为 true？**\n值在 -128~127 之间命中缓存池，是同一对象，结果为 true；超出则 false。比较包装类应使用 equals()。\n\n**3. 什么是自动装箱和拆箱？**\n基本类型与包装类之间的自动转换：装箱是 int→Integer，拆箱相反；拆箱时对象若为 null 会抛 NullPointerException。\n\n**4. String 为什么设计成不可变？**\n便于字符串常量池共享、天然线程安全、hashCode 可缓存（适合做 HashMap 的 key）；任何修改都产生新对象。\n\n**5. var 是动态类型吗？**\n不是。var 只是编译期推断出确定的静态类型，之后不可改变，且只能用于有初始值的局部变量。','generate://java-syntax','java','generated',0,'2026-09-03 20:49:21','2026-09-03 20:49:21',0,NULL,NULL),(250,'Java 流程控制','# Java 流程控制\n\n## 是什么\n\n程序默认一行一行从上往下执行，这叫“顺序结构”。流程控制就是给程序装上“拐弯”的能力：条件满足才执行、重复执行某段代码、或者中途提前退出去。\n\n大白话：顺序结构是“闷头往前走”，分支是“到路口看红绿灯”，循环是“绕操场跑十圈”，跳转是“跑到一半喊停”。用术语说，它是通过**分支语句、循环语句、跳转语句**改变代码执行顺序的语法。\n\n## 为什么重要\n\n- 没有流程控制，代码只能直线执行，任何带“判断”和“重复”的业务都写不出来，比如登录校验、批量处理订单。\n- 分支和循环是算法的骨架，学不扎实，后面数组、集合、算法全都吃力。\n- switch 新语法、break 标签是面试和代码评审中的高频细节，用错容易埋 bug。\n\n## 核心用法\n\n**1. if / else if / else（条件分支）**：从上往下依次判断，命中哪个执行哪个，都不命中走 else。条件必须是 boolean，写 `if (x = 1)` 会直接编译报错。\n\n**2. switch（Java 14+ 新语法）**：老写法用冒号 + break，忘了 break 会“穿透”到下一个 case，是经典 bug 来源。新语法用箭头 `->`：\n\n- 一个 case 可匹配多个值：`case 1, 2 ->`\n- 天然不穿透，不需要 break\n- 可作为表达式返回值，多行代码块用 `yield` 返回\n- 还支持模式匹配（Java 21）\n\n**3. for（计数循环）**：`for (初始化; 条件; 更新)`，适合已知次数的循环。\n\n**4. for-each（增强 for）**：`for (元素类型 变量 : 数组或集合)`，简洁但拿不到下标，遍历集合时不能结构性修改元素。\n\n**5. while / do-while**：while 先判断后执行，可能一次都不跑；do-while 先执行后判断，**至少跑一次**。\n\n**6. break / continue / 标签**：break 结束当前整层循环，continue 跳过本次进入下一轮。默认只作用于最内层循环，想一次跳出多层，在循环前写 `标签名:`，再用 `break 标签名;`。\n\n## 代码示例\n\n```java\npublic class FlowControlDemo {\n    public static void main(String[] args) {\n        // 1. if 分支\n        int score = 85;\n        if (score >= 90) {\n            System.out.println(\"优秀\");\n        } else if (score >= 60) {\n            System.out.println(\"及格\");\n        } else {\n            System.out.println(\"不及格\");\n        }\n\n        // 2. switch 新语法（Java 14+）\n        int day = 6;\n        String type = switch (day) {\n            case 1, 2, 3, 4, 5 -> \"工作日\";\n            case 6, 7 -> \"周末\";\n            default -> \"非法日期\";\n        };\n        System.out.println(type); // 周末\n\n        // 3. for：求 1~100 的和\n        int sum = 0;\n        for (int i = 1; i <= 100; i++) {\n            sum += i;\n        }\n        System.out.println(\"sum = \" + sum); // 5050\n\n        // 4. for-each：遍历数组\n        int[] nums = {10, 20, 30};\n        for (int n : nums) {\n            System.out.println(\"n = \" + n);\n        }\n\n        // 5. while\n        int i = 0;\n        while (i < 3) {\n            System.out.println(\"i = \" + i);\n            i++;\n        }\n\n        // 6. 带标签的 break：找到目标数就跳出全部循环\n        int[][] matrix = {{1, 2}, {3, 4}};\n        outer:                          // 标签写在要跳出的循环前面\n        for (int[] row : matrix) {\n            for (int v : row) {\n                if (v == 3) {\n                    System.out.println(\"找到了 \" + v);\n                    break outer;        // 结束外层循环，而不是只结束内层\n                }\n            }\n        }\n    }\n}\n```\n\n## 面试常问\n\n**Q1：switch 支持哪些数据类型？**\n支持 byte、short、char、int 及其包装类、枚举、String（Java 7 起）；不支持 long、float、double。\n\n**Q2：新旧 switch 语法有什么区别？**\n新语法（箭头式）天然不穿透、无需 break，支持多值匹配 `case 1, 2 ->`，还能作为表达式用 yield 返回值；老语法忘记 break 会穿透到下一个 case。\n\n**Q3：break 和 continue 的区别？**\nbreak 结束当前整层循环；continue 只结束本次迭代，继续下一轮。\n\n**Q4：for-each 遍历集合时能直接删除元素吗？**\n不能，会抛 ConcurrentModificationException，应使用 Iterator 的 remove 方法。\n\n**Q5：带标签的 break 是不是 goto？**\n不是。标签只能标注在包含它的循环或代码块上，且只能向外跳出，不能随意向前跳，比 goto 安全得多。','generate://java-control','java','generated',0,'2026-09-03 20:50:26','2026-09-03 20:50:26',0,NULL,NULL),(251,'Java 面向对象：类 / 对象 / 方法','# Java 面向对象：类 / 对象 / 方法\n\n## 是什么\n\n大白话：**类**是一张“图纸”，**对象**是照着图纸造出来的“实物”，**方法**是这个实物能干的“活儿”。比如“学生”是图纸，你身边的张三、李四就是对象；“学生能学习”就是方法。\n\n术语上：类是描述一类事物属性（成员变量）和行为（方法）的模板；对象是类的一个实例，通过 `new` 创建，在堆内存中分配空间，每个对象有独立的成员变量副本。\n\n## 为什么重要\n\nJava 是纯面向对象语言，一切代码都写在类里。掌握类与对象，才能理解封装、继承、多态三大特性；理解 `static` 和方法重载，才能看懂 JDK 源码和主流框架（如 Spring 的静态工厂、构造器注入），这是 Java 工程师的基本功。\n\n## 核心用法\n\n**1. 类定义**：`class 类名 { 成员变量; 方法; }`。成员变量描述状态，方法描述行为。\n\n**2. 构造器**：与类同名、无返回值（连 void 都不能写），在 `new` 时自动调用，用于初始化对象。不写任何构造器时，编译器自动生成一个无参构造器；一旦手写了构造器，默认的就没了。\n\n**3. this**：指向“当前对象”。两大用途：① 区分成员变量和同名参数（`this.name = name`）；② `this(...)` 在构造器第一行调用本类其他构造器，避免重复初始化代码。\n\n**4. static**：把成员从“对象级”升级为“类级”。静态变量全类共享一份；静态方法属于类，可直接用 `类名.方法()` 调用，且**不能访问实例变量、不能用 this**（因为静态方法调用时可能还没有对象）。\n\n**5. 方法重载**：同一个类中方法同名、**参数列表不同**（个数、类型或顺序不同）。与返回值和访问修饰符无关。重载让调用者不必记多个方法名，如 `println` 就有十几个重载版本。\n\n## 代码示例\n\n```java\npublic class Student {\n    // 实例变量：每个对象各有一份\n    private String name;\n    private int age;\n    // 静态变量：所有对象共享一份\n    private static int count = 0;\n\n    // 无参构造器：用 this() 复用下面的构造器，必须放首行\n    public Student() {\n        this(\"未知\", 0);\n    }\n\n    // 有参构造器（与无参构成重载）\n    public Student(String name, int age) {\n        this.name = name;  // this.name 是成员变量，name 是参数\n        this.age = age;\n        count++;\n    }\n\n    // 方法重载：同名不同参\n    public void study() {\n        System.out.println(name + \" 正在学习\");\n    }\n\n    public void study(String course) {\n        System.out.println(name + \" 正在学习 \" + course);\n    }\n\n    public void study(String course, int hours) {\n        System.out.println(name + \" 学习 \" + course + \" \" + hours + \" 小时\");\n    }\n\n    // 静态方法：属于类，用 类名.方法() 调用\n    public static int getCount() {\n        // 这里不能访问 name，因为静态方法中没有当前对象\n        return count;\n    }\n}\n```\n\n调用方式：\n\n```java\npublic class Test {\n    public static void main(String[] args) {\n        Student s1 = new Student(\"张三\", 20);\n        Student s2 = new Student();           // 走无参构造器\n        s1.study();\n        s1.study(\"Java\");                     // 编译器按参数自动匹配重载\n        System.out.println(Student.getCount()); // 输出 2\n    }\n}\n```\n\n## 面试常问\n\n**Q1：构造器可以被 static 或 final 修饰吗？**\n不可以。构造器属于对象初始化机制，不能被 static/final/abstract/synchronized 修饰，但可以私有（private），典型应用是单例模式。\n\n**Q2：静态方法中为什么不能用 this？**\nthis 指向当前对象，而静态方法随类加载、调用时不依赖任何对象，此时可能根本没有实例，所以编译器直接报错。\n\n**Q3：两个方法仅返回值类型不同，算重载吗？**\n不算。重载只看方法名和参数列表（个数、类型、顺序），返回值类型不同不构成重载，编译会报错。\n\n**Q4：重载和重写的区别？**\n重载发生在同一个类中，方法名相同、参数列表不同，编译期确定；重写发生在父子类之间，方法签名完全相同，运行期多态生效。\n\n**Q5：成员变量和局部变量重名时如何区分？**\n用 `this.变量名` 访问成员变量，不带 this 的是局部变量（就近原则）；构造器和 setter 中最常见。','generate://java-oop','java','generated',0,'2026-09-03 20:51:02','2026-09-03 20:51:02',0,NULL,NULL),(252,'Java 封装 / 继承 / 多态','# Java 封装 / 继承 / 多态详解\n\n## 是什么\n\n大白话：\n\n- **封装**：把数据和操作数据的方法装进同一个类，像一粒胶囊——外部不能直接伸手改内部数据，只能走你开放的“窗口”（getter/setter 方法）。\n- **继承**：子类自动获得父类的属性和方法，像孩子继承家产，不用重写一遍，还可以加自己的新东西。\n- **多态**：同一句代码，不同对象给出不同反应。猫和狗都会“叫”，但叫法不一样。\n\n用术语说：封装隐藏实现细节、保护数据；继承建立 is-a 关系、复用代码；多态靠“向上转型 + 方法重写 + 运行期动态绑定”，让一个引用在运行时表现出多种行为。\n\n## 为什么重要\n\n- **封装**：字段私有化后，可以在 setter 里加校验逻辑，数据不会被改乱；内部实现变了，外部调用不受影响。\n- **继承**：消除重复代码，形成清晰的类层次，方便统一管理公共行为。\n- **多态**：面向父类/接口编程，新增子类时几乎不改调用方代码，符合开闭原则，是 Spring 等框架回调、模板方法设计的基石。\n\n## 核心用法\n\n**1. 访问修饰符（封装的工具）**，可见范围从小到大：\n\n- `private`：仅本类可见，字段私有是封装的标准做法\n- 默认（不写）：同一个包内可见\n- `protected`：同包 + 其他包的子类可见\n- `public`：任何地方可见\n\n**2. extends 与 super（继承的工具）**\n\n- 子类用 `extends` 继承父类，Java 只支持单继承。\n- `super(...)` 调用父类构造方法，必须放在构造器第一行；不写的话编译器默认补一个 `super()`。\n- `super.方法()` 调用父类被重写的方法，`super.字段` 访问父类属性。\n\n**3. 方法重写规则（记“两同两小一大”）**\n\n- 两同：方法名、参数列表必须相同。\n- 两小：返回值相同或是其子类型；抛出的受检异常范围不能更大。\n- 一大：访问权限不能比父类更小（父类 public，子类不能改 protected）。\n- `private`、`static`、`final` 方法不能被重写；建议加 `@Override` 让编译器帮你检查。\n\n**4. 向上转型与动态绑定（多态的实现）**\n\n- 向上转型：`Parent p = new Child();`，安全，自动完成。\n- 动态绑定：调用 `p` 的重写方法时，JVM 在**运行时**根据对象的真实类型（new 的那个类）决定执行哪个版本。\n- 注意：成员变量、静态方法、private 方法**不发生多态**，编译期按引用类型决定。\n- 向下转型必须强制转换，先用 `instanceof` 判断，否则会抛 `ClassCastException`。\n\n## 代码示例\n\n```java\n// 封装：字段私有 + 公共访问入口\nclass Animal {\n    private String name;                 // 私有字段，外部无法直接访问\n\n    public Animal(String name) {\n        this.name = name;\n    }\n\n    public void setName(String name) {   // 封装入口，可加校验\n        if (name != null && !name.isEmpty()) {\n            this.name = name;\n        }\n    }\n\n    public void makeSound() {\n        System.out.println(name + \" 在叫\");\n    }\n}\n\n// 继承：Dog 是一种 Animal（is-a 关系）\nclass Dog extends Animal {\n    public Dog(String name) {\n        super(name);                     // 调用父类构造方法，必须在第一行\n    }\n\n    @Override                            // 编译器检查是否正确重写\n    public void makeSound() {\n        super.makeSound();               // 复用父类逻辑\n        System.out.println(\"汪汪！\");\n    }\n}\n\nclass Cat extends Animal {\n    public Cat(String name) {\n        super(name);\n    }\n\n    @Override\n    public void makeSound() {\n        System.out.println(\"喵喵！\");\n    }\n}\n\npublic class Demo {\n    public static void main(String[] args) {\n        // 向上转型：父类引用指向子类对象\n        Animal a1 = new Dog(\"旺财\");\n        Animal a2 = new Cat(\"咪咪\");\n\n        // 动态绑定：同一句代码，运行时各找各的实现\n        a1.makeSound();   // 旺财 在叫 / 汪汪！\n        a2.makeSound();   // 喵喵！\n\n        // 向下转型：先判断再强转\n        if (a1 instanceof Dog) {\n            Dog d = (Dog) a1;\n            d.makeSound();\n        }\n    }\n}\n```\n\n## 面试常问\n\n**Q1：private、默认、protected、public 的可见范围？**\nA：private 仅本类；默认仅同包；protected 同包 + 子类；public 任何地方。\n\n**Q2：重载和重写的区别？**\nA：重载是同类中方法名相同、参数不同，编译期确定；重写是子类覆盖父类同签名方法，运行期动态绑定。\n\n**Q3：super 有哪些用法？**\nA：调用父类构造方法（必须在第一行）、父类被重写的方法、父类字段。\n\n**Q4：`Animal a = new Dog();` 时调用的方法是谁的？**\nA：重写的方法执行 Dog 的版本（动态绑定）；但成员变量和静态方法按引用类型 Animal 决定，不发生多态。\n\n**Q5：子类构造方法第一行不写 super() 会怎样？**\nA：编译器自动插入无参 `super()`；若父类没有无参构造方法，则编译报错。','generate://java-oop-three','java','generated',0,'2026-09-03 20:51:55','2026-09-03 20:51:55',0,NULL,NULL),(253,'Java 接口与抽象类','# Java 接口与抽象类\n\n## 是什么\n\n大白话：**接口**像一份“合同”，只写“你必须会做什么”（比如“会飞”），不关心你怎么飞；**抽象类**像“半成品模板”，把公共部分先写好（有名字、会吃饭），再挖几个空留给子类填（怎么叫）。\n\n术语上：\n- **接口**：一组行为规范。方法默认 `public abstract`，变量默认 `public static final` 常量；Java 8 起还可包含 `default`、`static` 方法。类通过 `implements` 实现，且可实现多个。\n- **抽象类**：被 `abstract` 修饰的类，可同时包含抽象方法、具体方法、构造方法和任意字段；不能被 `new` 实例化，只能被继承。\n\n## 为什么重要\n\n- 面向接口编程是整个 Java 生态的基石：集合框架、JDBC、Spring 依赖注入都靠接口解耦。\n- 调用方只依赖规范不依赖具体实现，换实现不用改代码，可维护性和可测试性大幅提升。\n- 抽象类是代码复用的利器，是模板方法模式的基础。\n- 二者的区别与选型是 Java 面试必考题。\n\n## 核心用法\n\n**1. 选型口诀**\n- 用**接口**：描述“能做什么”，且需要让一个类同时具备多种能力时。\n- 用**抽象类**：描述“是什么”，多个子类要共享字段和公共逻辑时（模板复用）。\n\n**2. default 方法**：Java 8 起接口可写带方法体的默认实现，实现类不写也能直接用（也可覆写）。目的：给老接口加新方法时不破坏已有实现类，如 `List` 的 `sort`。\n\n**3. 接口多实现**：一个类只能 `extends` 一个类，但可以 `implements` 多个接口（逗号分隔）。若两个接口有同名 default 方法，实现类必须重写，并可用 `接口名.super.方法()` 指定调用版本。\n\n**4. Comparable**：实现 `Comparable<T>` 并重写 `compareTo`，对象就拥有“自然顺序”，可直接被 `Arrays.sort` 排序。\n\n## 代码示例\n\n一个例子覆盖四个重点：抽象类模板、default 方法、多实现、Comparable 排序。\n\n```Java\nimport java.util.Arrays;\n\n// 1. 抽象类：封装公共逻辑，留抽象方法给子类\nabstract class Animal {\n    protected String name;\n\n    public Animal(String name) {      // 抽象类可以有构造方法\n        this.name = name;\n    }\n\n    public void eat() {               // 具体方法：所有子类共用\n        System.out.println(name + \" 在吃东西\");\n    }\n\n    public abstract void makeSound(); // 抽象方法：子类必须实现\n}\n\n// 2. 接口：定义\"能力\"\ninterface Flyable {\n    void fly();\n}\n\ninterface Pet {\n    default void greet() {            // default 方法：自带默认实现\n        System.out.println(\"我是宠物，很高兴见到你\");\n    }\n}\n\n// 3. 继承一个抽象类 + 实现多个接口\nclass Bird extends Animal implements Flyable, Pet {\n    public Bird(String name) {\n        super(name);\n    }\n\n    @Override\n    public void makeSound() { System.out.println(name + \" 叽叽喳喳\"); }\n\n    @Override\n    public void fly() { System.out.println(name + \" 展翅飞翔\"); }\n}\n\n// 4. Comparable：定义自然排序\nclass Student implements Comparable<Student> {\n    String name;\n    int score;\n\n    public Student(String name, int score) {\n        this.name = name;\n        this.score = score;\n    }\n\n    @Override\n    public int compareTo(Student other) {\n        return Integer.compare(this.score, other.score); // 按分数升序\n    }\n\n    @Override\n    public String toString() { return name + \"(\" + score + \")\"; }\n}\n\npublic class Demo {\n    public static void main(String[] args) {\n        Bird bird = new Bird(\"小黄\");\n        bird.eat();\n        bird.makeSound();\n        bird.fly();\n        bird.greet();  // 直接使用接口的 default 方法\n\n        Student[] students = {\n            new Student(\"张三\", 88),\n            new Student(\"李四\", 95),\n            new Student(\"王五\", 72)\n        };\n        Arrays.sort(students); // 内部自动调用 compareTo\n        System.out.println(Arrays.toString(students));\n        // 输出：[王五(72), 张三(88), 李四(95)]\n    }\n}\n```\n\n## 面试常问\n\n**Q1：接口和抽象类有什么区别？**\n接口定义行为规范，方法默认 `public abstract`、字段默认 `public static final`；抽象类可有构造方法、成员变量和具体方法。类只能继承一个抽象类，但可实现多个接口。\n\n**Q2：什么时候用接口，什么时候用抽象类？**\n描述“能力”且需要多重能力组合时用接口；子类之间要共享代码和状态时用抽象类，两者也可搭配（抽象类实现接口的部分方法）。\n\n**Q3：default 方法是什么？为什么引入？**\nJava 8 让接口方法可带默认实现，方便在不破坏已有实现类的前提下给接口扩展新功能，典型如 `Collection` 的 `stream()`。\n\n**Q4：实现的两个接口有同名 default 方法怎么办？**\n实现类必须重写该方法消除冲突，需要时可用 `A.super.方法名()` 显式调用指定接口的版本。\n\n**Q5：Comparable 和 Comparator 有何区别？**\nComparable 是类自身实现的“自然排序”（`compareTo`）；Comparator 是外部比较器（`compare`），可为一个类定义多套排序规则，更灵活。','generate://java-interface','java','generated',0,'2026-09-03 20:53:07','2026-09-03 20:53:07',0,NULL,NULL),(254,'Java 集合框架：List / Set / Map','# Java 集合框架：List / Set / Map\n\n## 是什么\n\nJava 集合框架是 JDK 自带的“数据容器工具箱”，三大接口各司其职：\n\n- **List**：有序、可重复，像“排队的购物清单”，能按位置编号（下标）取元素。常用实现：ArrayList、LinkedList。\n- **Set**：无序、不可重复，像“花名册”，同一人只登记一次。常用实现：HashSet。\n- **Map**：键值对存储，像“字典”，通过“词”查“释义”。常用实现：HashMap。\n\n大白话：List 管“排队”，Set 管“去重”，Map 管“查表”。\n\n## 为什么重要\n\n日常开发中 80% 的数据处理都离不开集合：接口返回商品列表用 List，标签去重用 Set，缓存/计数用 Map。面试更是高频考点，尤其 ArrayList 与 LinkedList 的取舍、HashMap 底层原理，几乎是 Java 岗必问。理解它们的行为差异与性能特点，才能选对容器、写出高效代码。\n\n## 核心用法\n\n**1. ArrayList vs LinkedList**\n\n| 对比项 | ArrayList | LinkedList |\n|---|---|---|\n| 底层结构 | 动态数组 | 双向链表 |\n| 随机访问 | O(1)，按下标直达 | O(n)，从头遍历 |\n| 头部插入/删除 | O(n)，整体挪位 | O(1)，改指针 |\n| 内存 | 连续，缓存友好 | 每个节点额外存前后指针 |\n\n结论：读多写少首选 ArrayList（默认选择）；只有频繁在头尾插删才考虑 LinkedList，实际很少用。ArrayList 容量不足时按约 1.5 倍扩容并复制数组，预估大小可 `new ArrayList<>(size)` 避免反复扩容。\n\n**2. HashMap 原理概览**\n\nJDK 8+ 的 HashMap = 数组 + 链表 + 红黑树，put 流程：\n\n1. 对 key 的 hashCode 做扰动（高低位异或），再与（数组长度 - 1）按位与，定位下标；\n2. 槽位为空直接放入；发生哈希碰撞则尾插到链表；\n3. 链表长度 ≥ 8 且数组长度 ≥ 64 时，链表转红黑树，查询从 O(n) 降到 O(log n)；\n4. 元素数超过阈值（容量 × 负载因子 0.75）时容量翻倍并 rehash 迁移。\n\n**3. HashSet**\n\nHashSet 底层就是一个 HashMap——元素作 key，value 是一个固定的占位对象。它去重的本质是：先比 hashCode，再比 equals。自定义对象想去重，必须同时重写这两个方法。\n\n**4. 遍历方式与选型**\n\n- 普通 for（下标）：只适合 List；\n- 增强 for（for-each）：语法最简，所有集合通用，适合只读；\n- Iterator 迭代器：边遍历边删除的唯一安全方式；\n- forEach + Lambda：简洁，适合只读遍历。\n\n选型口诀：只读用 for-each；要删元素必须用 `iterator.remove()`，否则抛 ConcurrentModificationException；需要下标才用普通 for。\n\n## 代码示例\n\n```Java\nimport java.util.*;\n\npublic class CollectionDemo {\n    public static void main(String[] args) {\n        // List：有序、可重复\n        List<String> list = new ArrayList<>();\n        list.add(\"Java\");\n        list.add(\"Python\");\n        list.add(\"Java\");              // 允许重复\n        System.out.println(list);      // [Java, Python, Java]\n\n        // 迭代器遍历 + 安全删除\n        Iterator<String> it = list.iterator();\n        while (it.hasNext()) {\n            if (\"Python\".equals(it.next())) {\n                it.remove();          // for-each 中调 list.remove 会抛异常\n            }\n        }\n\n        // Set：去重\n        Set<String> set = new HashSet<>(list);\n        System.out.println(set);      // [Java]，重复元素被去掉\n\n        // Map：键值对\n        Map<String, Integer> map = new HashMap<>(8);\n        map.put(\"apple\", 3);\n        map.put(\"banana\", 5);\n        map.put(\"apple\", 10);         // 相同 key 覆盖旧值\n        map.forEach((k, v) -> System.out.println(k + \" -> \" + v));\n\n        Integer count = map.getOrDefault(\"pear\", 0); // 不存在时给默认值\n        System.out.println(\"pear = \" + count);\n    }\n}\n```\n\n## 面试常问\n\n**Q1：ArrayList 和 LinkedList 怎么选？**\nArrayList 随机访问 O(1)，适合读多写少；LinkedList 头尾插删 O(1)，但随机访问 O(n)。绝大多数场景选 ArrayList。\n\n**Q2：HashMap 链表什么时候转红黑树？**\n链表长度 ≥ 8 且数组容量 ≥ 64（否则优先扩容）；节点删到 ≤ 6 时退化回链表。\n\n**Q3：为什么重写 equals 必须同时重写 hashCode？**\nHashMap/HashSet 先用 hashCode 定位再用 equals 精确比较，只重写 equals 会导致“相等”的对象落到不同桶，被当成两个元素。\n\n**Q4：遍历时删除元素为什么不能用 for-each？**\nfor-each 底层依赖迭代器，直接调集合的 remove 会使 modCount 不一致，抛 ConcurrentModificationException，必须用 `iterator.remove()`。\n\n**Q5：HashMap 线程安全吗？**\n不安全，多线程 put 可能丢数据。并发场景用 ConcurrentHashMap，JDK 8 中用 CAS + synchronized 锁单个桶保证并发安全。','generate://java-collections','java','generated',0,'2026-09-03 20:53:53','2026-09-03 20:53:53',0,NULL,NULL),(255,'Java 异常体系','# Java 异常体系\n\n## 是什么\n\n大白话：程序运行时突然出的岔子，就像人突然生病——不处理就可能“挂掉”。Java 把这些岔子封装成对象，统称为异常。\n\n术语：异常是程序运行期发生的、打断正常执行流的事件，由 `Throwable` 及其子类描述，体系分三支：\n\n- `Error`：JVM 层的严重错误（如 `OutOfMemoryError`），程序管不了、也不该捕获；\n- **受检异常**：`Exception` 中除 `RuntimeException` 外的部分，如 `IOException`，编译器强制你处理；\n- **非受检异常**：`RuntimeException` 及其子类，如 `NullPointerException`，编译器不强制。\n\n## 为什么重要\n\n- **健壮性**：文件丢了、网络断了，程序不至于直接崩溃退出；\n- **解耦**：业务逻辑和出错处理分开写，主流程更清晰；\n- **契约**：方法签名上的 `throws` 是在告诉调用者“我可能出什么错”，属于 API 设计；\n- **面试高频**：受检 vs 非受检、finally 执行时机几乎是必考点。\n\n## 核心用法\n\n五个关键字：`try` 圈住可能出错的代码，`catch` 抓住处理，`finally` 收尾，`throw` 主动抛出，`throws` 声明转嫁给调用者。\n\n**1. 受检 vs 非受检**：受检异常代表“可预期的外部故障”（文件、网络、数据库），编译器逼你 try-catch 或向上 throws；非受检异常多代表“代码写错了”（空指针、越界），应靠修代码解决，而不是到处 catch。\n\n| 维度 | 受检异常 | 非受检异常 |\n|---|---|---|\n| 父类 | Exception（不含 RuntimeException） | RuntimeException / Error |\n| 编译器 | 强制处理或声明 | 不强制 |\n| 典型例子 | IOException、SQLException | NullPointerException、IllegalArgumentException |\n\n**2. finally 执行时机**：只要 JVM 正常运行，无论 try 正常结束、执行了 return、还是 catch 捕获了异常，finally 都会在“方法真正返回之前”执行。不执行的情形：`System.exit()`、JVM 崩溃或被杀、finally 自身死循环。\n\n**3. try-with-resources**：资源类（文件、连接）实现了 `AutoCloseable` 接口后，声明在 try 的括号里，编译器会自动调用 `close()`，多个资源按“后开先关”的逆序关闭，比手写 finally 更安全简洁。\n\n**4. 自定义异常**：系统自带异常表达不了业务语义时（如“余额不足”），继承 `RuntimeException`（主流做法，不强制调用方处理）或 `Exception`（强制处理），并携带错误码等字段。\n\n## 代码示例\n\n```Java\n// 1. finally 在 return 之前执行\npublic class FinallyDemo {\n    static int demo() {\n        try {\n            return 1;\n        } finally {\n            System.out.println(\"finally 执行了\"); // 先打印，再返回 1\n        }\n    }\n}\n```\n\n```Java\n// 2. try-with-resources：自动关闭资源，无需手写 finally\nimport java.io.*;\n\npublic class ResourceDemo {\n    public static void main(String[] args) {\n        try (BufferedReader br = new BufferedReader(new FileReader(\"data.txt\"))) {\n            System.out.println(br.readLine());\n        } catch (IOException e) {  // FileReader 的受检异常，必须处理\n            System.out.println(\"读文件失败：\" + e.getMessage());\n        }\n    }\n}\n```\n\n```Java\n// 3. 自定义业务异常\nclass BusinessException extends RuntimeException {  // 非受检，调用方无需强制捕获\n    private final int code;\n    public BusinessException(int code, String message) {\n        super(message);\n        this.code = code;\n    }\n    public int getCode() { return code; }\n}\n\npublic class BankDemo {\n    public static void withdraw(double balance, double amount) {\n        if (amount > balance) {\n            throw new BusinessException(4001, \"余额不足\"); // 主动抛出\n        }\n    }\n}\n```\n\n## 面试常问\n\n1. **受检异常和非受检异常的区别？**\n   受检异常编译器强制 try-catch 或 throws，代表可预期外部故障（IOException）；非受检异常编译器不管，多代表程序缺陷（NPE）。\n\n2. **finally 一定会执行吗？**\n   只要 JVM 正常运行就一定执行（包括 try/catch 中有 return 的场景）；但 `System.exit()`、JVM 崩溃、finally 死循环时不执行。\n\n3. **try 里 return 了，finally 还执行吗？顺序如何？**\n   执行，且在方法真正返回前执行；若 finally 里也写 return，会覆盖待返回的值——属于反模式，禁止使用。\n\n4. **try-with-resources 的原理？**\n   资源需实现 `AutoCloseable`，编译器自动生成在 finally 中调用 close() 的代码，多个资源逆序关闭，原异常也不会被 close 的异常吞掉（作为 suppressed 保留）。\n\n5. **什么时候自定义异常？父类怎么选？**\n   系统异常无法表达业务语义时自定义；一般继承 `RuntimeException`（主流规范推荐），需要强制调用方处理时才继承 `Exception`。','generate://java-exceptions','java','generated',0,'2026-09-03 20:55:09','2026-09-03 20:55:09',0,NULL,NULL),(256,'Java 泛型','# Java 泛型\n\n## 是什么\n\n大白话：泛型就是给类型“挖个坑”。写代码时先用一个占位符（比如 `T`）占着位置，等到真正使用时，再把具体类型填进去。好比方法参数是“值的坑”，泛型就是“类型的坑”。\n\n术语：泛型允许在定义类、接口、方法时声明**类型参数**，在实例化或调用时才传入真实类型。编译器会在编译期检查类型是否匹配，从而保证类型安全。\n\n## 为什么重要\n\n没有泛型时，集合只能存 `Object`，取出来必须强制转换，一旦放错了类型，程序运行到强转那一行才崩溃。泛型把这类错误提前到编译期：\n\n1. **编译期类型检查**：错误早暴露，不用等运行时抛 `ClassCastException`；\n2. **省去强制转换**：取出的元素天然就是目标类型，代码更干净；\n3. **一套代码复用**：`Box<T>` 可以同时适配 `String`、`Integer` 等所有类型。\n\n## 核心用法\n\n**1. 泛型类**：在类名后声明类型参数 `class Box<T>`，使用时指定 `new Box<String>()`，T 在类内部当作类型用。\n\n**2. 泛型方法**：类型参数写在返回值之前，如 `public static <T> T first(List<T> list)`，调用时编译器自动推断 T 的类型。\n\n**3. 有界类型参数**：`<T extends Number>` 限制 T 必须是 Number 或其子类，这样代码里就能安全调用 Number 的方法。\n\n**4. 通配符**：\n\n- `? extends T`（上界通配符）：元素类型是 T 或其子类。只能**读**，不能写（除了 null）——因为编译器不知道具体是哪个子类，写进去就可能不安全。\n- `? super T`（下界通配符）：元素类型是 T 或其父类。只能**写** T 及其子类，读出来只能当 `Object`。\n- 记忆口诀 **PECS**：Producer Extends（要读数据用 extends），Consumer Super（要写数据用 super）。\n\n**5. 类型擦除**：泛型只存在于编译期。编译后 T 被替换为 `Object`（有界则替换为上界），运行时 JVM 感知不到泛型。因此不存在 `List<String>.class`，也不能 `new T()`、`obj instanceof List<String>`。\n\n## 代码示例\n\n泛型类：\n\n```Java\n// 泛型类：T 是类型参数\nclass Box<T> {\n    private T value;\n\n    public void set(T value) { this.value = value; }\n    public T get() { return value; }\n}\n\nBox<String> box = new Box<>();\nbox.set(\"hello\");        // 只能放 String\nString s = box.get();     // 不需要强制转换\n// box.set(123);         // 编译报错：错误在编译期就暴露\n```\n\n泛型方法与有界类型：\n\n```Java\n// 泛型方法：<T> 写在返回值之前\npublic static <T> T first(List<T> list) {\n    return list.get(0);\n}\n\n// 有界类型参数：T 必须是 Number 或其子类\npublic static <T extends Number> double sum(List<T> list) {\n    double total = 0;\n    for (T t : list) {\n        total += t.doubleValue(); // 可安全调用 Number 的方法\n    }\n    return total;\n}\n```\n\n通配符与 PECS：\n\n```Java\n// ? extends：只读（生产者）\npublic static double sumAll(List<? extends Number> list) {\n    double total = 0;\n    for (Number n : list) {\n        total += n.doubleValue();\n    }\n    // list.add(1); // 编译错误：不能写入\n    return total;\n}\n\n// ? super：只写（消费者）\npublic static void fillZero(List<? super Integer> list) {\n    for (int i = 0; i < 3; i++) {\n        list.add(0); // 可以安全写入 Integer\n    }\n    // Integer x = list.get(0); // 编译错误：读出只能当 Object\n}\n\n// PECS 典型应用：从 src 读（extends），往 dest 写（super）\npublic static void copy(List<? extends Number> src,\n                        List<? super Number> dest) {\n    for (Number n : src) {\n        dest.add(n);\n    }\n}\n```\n\n类型擦除的体现：\n\n```Java\nList<String> a = new ArrayList<>();\nList<Integer> b = new ArrayList<>();\nSystem.out.println(a.getClass() == b.getClass()); // true，运行时都是 List\n```\n\n## 面试常问\n\n**1. 什么是类型擦除？**\n泛型信息只保留到编译期，编译后类型参数被替换为 `Object`（或上界），运行时拿不到。因此不能 `new T()`、不能 `T.class`、不能 `instanceof List<String>`，泛型类也不能继承 `Throwable`。\n\n**2. `List<? extends Number>` 和 `List<? super Integer>` 的区别？**\n前者是上界通配符，只能读不能写；后者是下界通配符，只能写、读出是 `Object`。口诀：PECS，生产者用 extends，消费者用 super。\n\n**3. 为什么 `List<String>` 不能赋值给 `List<Object>`？**\nJava 泛型是不变的。如果允许，就能往 `List<Object>` 里放 Integer，再按 String 取出，类型安全被破坏，所以编译器直接禁止。\n\n**4. 泛型能用基本类型吗？**\n不能，`List<int>` 非法。因为擦除后类型参数变为 `Object`，而基本类型不是对象，必须用包装类如 `List<Integer>`。\n\n**5. 类型擦除后还能拿到泛型信息吗？**\n对象运行时类型拿不到，但声明处的泛型（字段、方法签名、父类泛型）会保留在字节码中，可通过 `getGenericSuperclass()` 等反射 API 读取。','generate://java-generics','java','generated',0,'2026-09-03 20:56:12','2026-09-03 20:56:12',0,NULL,NULL),(257,'Java IO 流基础','# Java IO 流基础\n\n## 是什么\n\n大白话：程序经常要和外部打交道——读文件、写文件、网络传输。数据像水流一样在程序和外部之间“流动”，Java 把这套管道统称为 IO 流。\n\n术语：`java.io` 包提供了一套读写数据的 API。按方向分：输入流（读进来）和输出流（写出去）；按数据处理单位分：字节流（以 byte 为单位，基类 InputStream/OutputStream）和字符流（以 char 为单位，基类 Reader/Writer）。\n\n## 为什么重要\n\n- 文件读写、日志记录、配置加载、网络通信，几乎所有真实项目都离不开 IO。\n- 面试高频：字节流 vs 字符流、缓冲流原理、资源关闭，都是必考点。\n- 缓冲流是“装饰器模式”的经典实现，理解它是看懂 Java 类库设计的敲门砖。\n- 用不好会出现资源泄漏、中文乱码、性能低下等实际问题。\n\n## 核心用法\n\n**1. 字节流 vs 字符流**\n\n- 字节流能处理一切文件：图片、视频、压缩包、文本都行。\n- 字符流专为文本设计，能按指定字符集解码，天然避免中文乱码。\n- 记忆口诀：**纯文本用字符流，其他一律字节流**。\n\n**2. 缓冲流 BufferedReader / BufferedWriter**\n\n- 基础流每次 read() 都可能触发系统调用，非常慢；缓冲流内部有一个默认 8KB 的数组，一次读一大块，大幅减少系统调用次数。\n- `readLine()` 按行读文本，`newLine()` 写跨平台换行符，是读写文本的首选。\n- 本质是装饰器模式：缓冲流“包住”基础流，增强其功能。\n\n**3. try-with-resources 关流**\n\n- 流实现了 `AutoCloseable` 接口，`try (资源) { ... }` 语法在代码块结束时自动调用 close()，**即使中途抛异常也保证关闭**。\n- 多个资源用分号分隔，关闭顺序与声明顺序相反。\n- 替代传统 try-finally 手动关流，代码更简洁，杜绝资源泄漏。\n\n## 代码示例\n\n```java\nimport java.io.*;\n\npublic class IODemo {\n    public static void main(String[] args) {\n        // 1. 读文本：字符流 + 缓冲\n        try (BufferedReader reader = new BufferedReader(\n                new FileReader(\"input.txt\"))) {\n            String line;\n            while ((line = reader.readLine()) != null) { // 读到末尾返回 null\n                System.out.println(line);\n            }\n        } catch (IOException e) {\n            e.printStackTrace();\n        }\n\n        // 2. 写文本：try-with-resources 自动关流\n        try (BufferedWriter writer = new BufferedWriter(\n                new FileWriter(\"output.txt\"))) {\n            writer.write(\"Hello, Java IO!\");\n            writer.newLine();\n            writer.write(\"第二行\");\n        } catch (IOException e) {\n            e.printStackTrace();\n        }\n\n        // 3. 复制文件：字节流 + 缓冲（适用于任意文件类型）\n        try (BufferedInputStream in = new BufferedInputStream(\n                new FileInputStream(\"a.jpg\"));\n             BufferedOutputStream out = new BufferedOutputStream(\n                     new FileOutputStream(\"b.jpg\"))) {\n            byte[] buf = new byte[8192];\n            int len;\n            while ((len = in.read(buf)) != -1) { // 读到末尾返回 -1\n                out.write(buf, 0, len);\n            }\n        } catch (IOException e) {\n            e.printStackTrace();\n        }\n    }\n}\n```\n\n## 面试常问\n\n**Q1：字节流和字符流的区别？**\n字节流以 byte 为单位，可处理任意文件；字符流以 char 为单位，专为文本设计，可指定编码避免乱码。\n\n**Q2：为什么 BufferedReader 比直接用 FileReader 快？**\n内部维护缓冲区，一次读一大块数据，减少频繁的系统调用，并提供 readLine() 按行读取能力。\n\n**Q3：try-with-resources 的作用是什么？**\n自动关闭实现了 AutoCloseable 的资源，即使抛异常也保证 close() 执行，替代 try-finally，避免资源泄漏。\n\n**Q4：read() 返回 -1、readLine() 返回 null 分别代表什么？**\n都表示流已读到末尾，是循环读取的通用结束条件；readLine() 返回空串 \"\" 则表示读到了空行。\n\n**Q5：缓冲流体现了什么设计模式？**\n装饰器模式，缓冲流包装基础流并增强其功能，不改变原流的结构。','generate://java-io','java','generated',0,'2026-09-03 20:57:02','2026-09-03 20:57:02',0,NULL,NULL),(258,'Java 多线程基础','# Java 多线程基础\n\n## 是什么\n\n多线程就是让一个程序“同时”干好几件事。大白话：厨房里只有一位厨师时，洗菜、切菜、炒菜只能排队做；多线程相当于多雇几位厨师同时开工，互不等待。用术语说：线程是 CPU 调度的最小执行单位，运行 main 方法的就是“主线程”，Java 允许我们在程序中再创建多个线程并发执行不同任务。\n\n## 为什么重要\n\n1. **榨干多核 CPU**：现代 CPU 都是 4 核 8 核起步，单线程同一时刻只能用一个核，多线程能真正并行干活，提升吞吐量。\n2. **不卡主流程**：下载文件、调用接口这类耗时操作丢给后台线程，界面不白屏、主流程不阻塞。\n3. **后续进阶的地基**：并发集合、锁原理、各种框架的异步能力，全都建立在这些概念上，也是面试必考点。\n\n代价：线程创建销毁有开销；多个线程共享数据时会出现“线程安全”问题，需要额外保护。\n\n## 核心用法\n\n**1. 创建线程：Thread 与 Runnable**\n- 继承 `Thread`，重写 `run()`，调用 `start()` 启动。注意：直接调 `run()` 不会开新线程，只是普通方法调用。\n- 实现 `Runnable`（任务），交给 `Thread` 执行。推荐后者：不占用唯一的继承名额、任务与线程解耦、方便提交给线程池。\n\n**2. 线程安全与 synchronized**\n多个线程同时修改一个共享变量，结果可能出错。经典案例：两个线程各执行 1000 次 `count++`，结果往往小于 2000。因为 `count++` 不是原子操作，分“读→改→写”三步，会被其他线程插队。`synchronized` 就是加锁：同一时刻只放一个线程进临界区，兼顾原子性、可见性、有序性。可修饰方法或代码块 `synchronized (lock) { ... }`。\n\n**3. volatile：轻量但只管一半**\n两个作用：可见性（一个线程改了值，其他线程立刻看到新值）和禁止指令重排。但**不保证原子性**——`count++` 加了 volatile 照样丢数据。适合“一写多读”场景，如停止线程的开关标志。\n\n**4. 线程池 ExecutorService**\n线程很“贵”，频繁创建销毁浪费资源。线程池让线程复用，还能控制数量、让任务排队。入门用 `Executors.newFixedThreadPool(3)` 创建固定大小线程池；生产环境推荐 `new ThreadPoolExecutor(...)` 手动指定参数（队列长度、拒绝策略）避免 OOM。用 `submit()` 提交任务，用完 `shutdown()` 关闭。\n\n## 代码示例\n\n```Java\nimport java.util.concurrent.*;\n\npublic class MultiThreadDemo {\n\n    // 1. 实现 Runnable 定义任务（推荐写法）\n    static class MyTask implements Runnable {\n        public void run() {\n            System.out.println(Thread.currentThread().getName() + \" 在执行任务\");\n        }\n    }\n\n    // 2. 共享计数器：不加锁，两个线程各加 1000 次会丢数据\n    static class Counter {\n        private int count = 0;\n        public synchronized void increment() { count++; } // 上锁保证原子性\n        public int getCount() { return count; }\n    }\n\n    // 3. volatile 开关：用于优雅停止线程\n    static class Worker implements Runnable {\n        private volatile boolean running = true;\n        public void run() {\n            while (running) { /* 持续干活 */ }\n            System.out.println(\"收到停止信号，线程安全退出\");\n        }\n        void stop() { running = false; }\n    }\n\n    public static void main(String[] args) throws Exception {\n        // ---- Thread 与 Runnable：必须 start() 才会开新线程 ----\n        new Thread(new MyTask(), \"线程A\").start();\n        new Thread(() -> System.out.println(\"Lambda 写法更简洁\"), \"线程B\").start();\n\n        // ---- synchronized：并发累加 ----\n        Counter counter = new Counter();\n        Runnable add = () -> { for (int i = 0; i < 1000; i++) counter.increment(); };\n        Thread t1 = new Thread(add), t2 = new Thread(add);\n        t1.start(); t2.start();\n        t1.join(); t2.join(); // 主线程等待子线程结束\n        System.out.println(\"加锁后结果稳定为 2000：\" + counter.getCount());\n\n        // ---- 线程池：复用线程 ----\n        ExecutorService pool = Executors.newFixedThreadPool(3);\n        Future<Integer> f = pool.submit(() -> 1 + 1); // 提交有返回值的任务\n        System.out.println(\"任务结果：\" + f.get());   // 阻塞获取结果\n        Worker worker = new Worker();\n        pool.submit(worker);\n        Thread.sleep(100);\n        worker.stop(); // 没加 volatile 的话，工作线程可能永远看不到这次修改\n        pool.shutdown();\n    }\n}\n```\n\n## 面试常问\n\n**Q1：Thread 和 Runnable 的区别？**\nThread 代表线程本身，Runnable 代表任务。实现 Runnable 不占用继承名额、任务与线程解耦，还能直接交给线程池，优先选择。\n\n**Q2：start() 和 run() 的区别？**\nstart() 由 JVM 创建新线程并回调 run()；直接调 run() 只是在当前线程里执行普通方法，不产生新线程。\n\n**Q3：synchronized 和 volatile 的区别？**\nvolatile 只保证可见性和有序性，不保证原子性，开销小，适合状态标志；synchronized 三者都保证，是互斥锁，适合保护复合操作。\n\n**Q4：为什么要用线程池？**\n避免频繁创建销毁线程的开销，控制并发数量，让任务排队缓冲。生产推荐手动 new ThreadPoolExecutor 定制参数，规避 Executors 默认无界队列的 OOM 风险。\n\n**Q5：count++ 为什么加 volatile 不够，必须加锁？**\ncount++ 分读、改、写三步，非原子，并发下会互相覆盖；volatile 只保证可见性，无法让三步不被打断，所以要用 synchronized 或 AtomicInteger。','generate://java-threads','java','generated',0,'2026-09-03 20:58:51','2026-09-03 20:58:51',0,NULL,NULL),(259,'Java Lambda 与 Stream','# Java Lambda 与 Stream 入门\n\n## 是什么\n\nLambda 是 Java 8 引入的“匿名函数”写法。大白话：以前想把一段“行为”传给别人，必须先定义类或匿名内部类把它包起来；现在可以直接把代码当数据传来传去，写作 `(参数) -> 表达式`。\n\nStream 是配合 Lambda 的数据处理管道。大白话：把集合想象成水管里的水，Stream 就是管道，数据从一头流入，经过一个个“阀门”（过滤、转换、汇总），最后从另一头流出结果。\n\n术语：Lambda 本质是函数式接口的简洁实现；Stream 是对集合等数据源进行声明式、惰性求值处理的流水线。\n\n## 为什么重要\n\n1. 代码更短：集合处理常从十几行 for 循环缩到一两行。\n2. 声明式编程：只说“做什么”，不说“怎么做”，可读性高。\n3. 易于并行：`parallelStream()` 一行代码利用多核 CPU。\n4. 行业标配：Spring 等主流框架和面试都默认你掌握。\n\n## 核心用法\n\n### 1. 函数式接口\n\n只有一个抽象方法的接口，可加 `@FunctionalInterface` 标注。Java 内置四大金刚：\n\n- `Predicate<T>`：传入 T，返回 boolean —— 用于判断、过滤\n- `Function<T, R>`：传入 T，返回 R —— 用于转换\n- `Consumer<T>`：传入 T，无返回 —— 用于消费\n- `Supplier<T>`：无输入，返回 T —— 用于生产\n\nLambda 就是给这类接口赋值的简写。\n\n### 2. Stream 三步曲\n\n1. 获取流：`list.stream()`\n2. 中间操作（惰性，返回新 Stream，可链式调用）：`filter` 过滤、`map` 转换、`sorted` 排序、`distinct` 去重、`limit` 截断\n3. 终止操作（触发真正执行）：`collect` 收集、`forEach` 遍历、`count` 计数、`reduce` 归约、`anyMatch` 判断\n\n### 3. 与 for 循环对比\n\nfor 是命令式：自己管理下标、临时变量和遍历细节。Stream 是声明式：像写 SQL 一样描述结果。“遍历 + 判断 + 收集”的组合场景 Stream 明显更简洁；但简单自增遍历、需要复杂提前中断或性能极致敏感的场景，for 仍有优势。\n\n## 代码示例\n\n需求：筛选金额大于 100 的订单，取名称，去重后收集为 List。\n\n传统 for 写法：\n\n```java\nList<String> result = new ArrayList<>();\nSet<String> seen = new HashSet<>();\nfor (Order o : orders) {\n    if (o.getAmount() > 100 && seen.add(o.getName())) {\n        result.add(o.getName());\n    }\n}\n```\n\nLambda + Stream 写法：\n\n```java\nList<String> result = orders.stream()\n        .filter(o -> o.getAmount() > 100)   // Predicate：过滤\n        .map(Order::getName)                // Function：提取字段\n        .distinct()\n        .collect(Collectors.toList());      // 终止：收集结果\n```\n\n函数式接口用法：\n\n```java\nPredicate<Integer> isEven = n -> n % 2 == 0;          // 判断\nFunction<String, Integer> len = String::length;       // 转换\nConsumer<String> printer = s -> System.out.println(s); // 消费\nSupplier<List<String>> maker = ArrayList::new;        // 生产\n```\n\n其他常用操作：\n\n```java\n// 按城市分组\nMap<String, List<Customer>> byCity = customers.stream()\n        .collect(Collectors.groupingBy(Customer::getCity));\n\n// 金额求和\nint total = orders.stream().mapToInt(Order::getAmount).sum();\n\n// 按分数降序取前 3 名\nList<Student> top3 = students.stream()\n        .sorted(Comparator.comparing(Student::getScore).reversed())\n        .limit(3)\n        .collect(Collectors.toList());\n```\n\n## 面试常问\n\n1. **Lambda 的本质是什么？**\n   它是函数式接口的实例，编译后生成私有方法配合 invokedynamic 指令，运行时动态生成实现，并非匿名内部类的简单语法糖（不额外生成 .class 文件）。\n\n2. **中间操作和终止操作的区别？**\n   中间操作是惰性的，只记录不执行；终止操作触发管道一次性遍历，Stream 用完即废、不可复用。\n\n3. **filter 和 map 的区别？**\n   filter 按条件筛选，元素数量可能变少但元素本身不变；map 逐个转换，数量不变但类型可能变。\n\n4. **Stream 和 for 循环怎么选？**\n   集合的过滤、转换、汇总等声明式处理用 Stream，更简洁且易并行；简单遍历或需频繁中断、修改外部状态时用 for。\n\n5. **什么是 Stream 的短路操作？**\n   如 `limit`、`findFirst`、`anyMatch`，找到满足条件的结果就停止处理后续元素，类似循环里的 break。','generate://java-lambda-stream','java','generated',0,'2026-09-03 20:59:44','2026-09-03 20:59:44',0,NULL,NULL),(260,'C 语言基本语法与数据类型','# C 语言基本语法与数据类型\n\n## 是什么\n\n大白话：C 语言里，你要存数据就得先告诉计算机三件事——存什么类型、叫什么名字、能不能改。变量是“贴了标签、可以换内容的盒子”，常量是“封死的盒子”，数据类型决定“盒子多大、能装什么”。\n\n术语：数据类型规定了数据的存储大小、取值范围与可参与的操作；基本语法则涵盖变量声明、常量定义、格式化输入输出、类型别名等语言规则。\n\n## 为什么重要\n\n- 变量、类型、输入输出是后续数组、指针、结构体的地基，概念不清后面寸步难行。\n- 理解整型大小与溢出能避免真实工程 bug（整数溢出攻击、经典 Y2K/2038 问题都源于此）。\n- printf/scanf 是初学者调试和交互的第一工具。\n- typedef 广泛出现在开源代码中（如 `size_t`、`int32_t`），看不懂它就读不了别人的代码。\n\n## 核心用法\n\n### 1. 变量与常量\n\n- 变量：`int age = 18;` 先写类型、再起名字，之后可反复赋值。\n- 常量三种写法：\n  - `const int MAX = 100;`（有类型、有作用域，编译器检查，推荐）\n  - `#define PI 3.14`（预处理阶段的纯文本替换，无类型检查）\n  - `enum { OK = 0 };`（适合一组相关整数常量）\n- 命名规则：只能含字母、数字、下划线；不能以数字开头；区分大小写；不能用关键字（如 `int`、`return`）。\n\n### 2. 整型家族与溢出\n\n整型按“长度 × 符号”分家族（64 位 Linux 典型值，平台有差异）：\n\n| 类型 | 典型大小 | 说明 |\n|---|---|---|\n| short | 2 字节 | 省内存的小整数 |\n| int | 4 字节 | 默认整数类型 |\n| long | 8 字节 | 平台差异大（Windows 上常为 4） |\n| long long | 8 字节 | C99 起保证至少 64 位 |\n\n加 `unsigned` 变无符号（只存非负数，范围翻倍）。**溢出**：超出范围会“绕回去”——无符号溢出按模 2ⁿ 回绕（有定义）；有符号溢出是**未定义行为**，结果不可预测。建议用 `<limits.h>` 查上限、用 `<stdint.h>` 的 `int32_t` 等定宽类型保证可移植。\n\n### 3. printf / scanf\n\n- `printf(\"格式串\", 参数...)`：常用占位符 `%d`(int)、`%u`(无符号)、`%f`(float/double)、`%c`(字符)、`%s`(字符串)、`%zu`(sizeof 结果)、`%p`(指针)。\n- `scanf(\"%d\", &x)`：**必须加 `&` 取地址**，因为要把键盘读到的数据写进变量的内存；它返回成功读取的项数，检查返回值是好习惯。\n\n### 4. typedef\n\n给已有类型起别名：`typedef unsigned long long u64;`，提高可读性、屏蔽平台差异，是写跨平台代码的标准手段。\n\n## 代码示例\n\n```C++\n#include <stdio.h>\n#include <limits.h>\n\ntypedef unsigned char byte;   // typedef：给类型起别名\n\nint main(void) {\n    // 1. 变量与常量\n    int score = 95;           // 变量：可以修改\n    const int PASS = 60;      // 常量：只读，再赋值会编译报错\n    score = score + 5;        // 合法\n    printf(\"score=%d, PASS=%d\\n\", score, PASS);\n\n    // 2. 整型家族与溢出\n    short s = 32767;          // short 的上限\n    s = (short)(s + 1);       // 有符号溢出：未定义行为，常\"绕回\"为 -32768\n    unsigned char u = 255;\n    u = (unsigned char)(u + 1); // 无符号回绕：模 256，结果为 0\n    printf(\"s=%d, u=%u\\n\", s, u);\n\n    printf(\"int 大小 %zu 字节，最大值 %d\\n\", sizeof(int), INT_MAX);\n\n    // 3. scanf 输入\n    int age = 0;\n    printf(\"请输入年龄: \");\n    if (scanf(\"%d\", &age) == 1) {      // 注意 & 取地址，并检查返回值\n        printf(\"明年你 %d 岁\\n\", age + 1);\n    }\n\n    // 4. typedef 使用\n    byte b = 200;\n    printf(\"byte b = %u\\n\", b);\n    return 0;\n}\n```\n\n## 面试常问\n\n**Q1：const 和 #define 定义常量有什么区别？**\nconst 有类型和作用域，由编译器检查；#define 是预处理文本替换，无类型检查，容易引入隐蔽错误。\n\n**Q2：int 溢出会发生什么？**\n有符号整数溢出是未定义行为，结果不可预测；无符号溢出按模 2ⁿ 回绕，是语言明确定义的。\n\n**Q3：scanf 的变量前为什么要加 &？**\nscanf 需要变量的内存地址才能把数据写进去，& 是取地址符；漏写是初学阶段最常见的段错误来源。\n\n**Q4：typedef 和 #define 有何区别？**\ntypedef 是编译器理解的类型别名；#define 只是文本替换。例如 `#define PTR int*` 后 `PTR a, b;` 中 b 实际是 int 而非 int*。\n\n**Q5：printf 里 %d 和 %u 的区别？**\n%d 按有符号十进制输出，%u 按无符号输出；格式符与实参类型不匹配会打印出错误值（如用 %d 输出 -1 的 unsigned 会得到 -1 而非巨大正数）。','generate://c-syntax','cpp','generated',0,'2026-09-03 21:00:41','2026-09-03 21:00:41',0,NULL,NULL),(261,'C 指针详解','# C 指针详解\n\n## 是什么\n\n大白话：指针就是一张“写着门牌号的纸条”。变量住在内存的某个“房间”里，指针本身也是个变量，但它存的不是普通数据，而是那个房间的“门牌号”（地址）。拿着纸条，你就能找到并修改房间里的东西。\n\n术语：指针是存储内存地址的变量。声明时的类型（如 `int*`）表示它指向什么类型的数据，决定了解引用的方式和指针加减运算的步长。\n\n## 为什么重要\n\n- C/C++ 操作内存的基石：`malloc`/`new` 返回的就是指针，动态内存全靠它管理。\n- 函数默认是值传递，想在函数里“真正修改”外部变量，必须传地址。\n- 数组、字符串、链表、树等数据结构的底层实现都依赖指针。\n- 面试高频考点，不懂指针等于没学过 C。\n\n## 核心用法\n\n**1. 取地址与解引用**\n`&` 取地址：拿到变量在内存中的地址；`*` 解引用：顺着地址找到那个变量，可读可写。两者互为逆操作。\n\n**2. 指针与数组**\n数组名在大多数表达式中会“退化”为指向首元素的指针，所以 `arr[i]` 等价于 `*(arr + i)`，指针也能用 `p[i]` 下标访问。区别：数组名是地址常量不能重新赋值，且 `sizeof(数组名)` 是整个数组大小，`sizeof(指针)` 是指针自身大小。\n\n**3. 二级指针**\n指针的指针，存的是“指针变量的地址”。典型场景：C 是值传递，想在函数内修改外部的“指针本身”（如动态分配、释放后置空），就得传指针的地址进去。\n\n**4. 空指针与野指针**\n空指针（`NULL` / `nullptr`）不指向任何对象，使用前必须判空。野指针是“指向不明”的指针：未初始化的指针、或指向已释放/已失效内存的指针，解引用是未定义行为。避免方法：定义即初始化、`free`/`delete` 后立即置空。\n\n**5. void***\n通用指针，能接收任意类型的地址（如 `malloc` 的返回值），但它不知道指向的数据多大、是什么类型，不能直接解引用或加减运算，必须先强制转换成具体类型指针。\n\n## 代码示例\n\n```C++\n#include <cstdio>\n#include <cstdlib>\n\nint main() {\n    // 1. 取地址与解引用\n    int a = 10;\n    int* p = &a;              // & 取地址\n    *p = 20;                  // * 解引用，a 变成 20\n    printf(\"a = %d\\n\", a);\n\n    // 2. 指针与数组\n    int arr[5] = {1, 2, 3, 4, 5};\n    int* q = arr;             // 数组名退化为首元素地址\n    printf(\"%d %d\\n\", q[0], *(q + 2));      // 1 和 3\n    printf(\"%zu %zu\\n\", sizeof(arr), sizeof(q)); // 20 和 8（64 位系统）\n\n    // 3. 二级指针\n    int** pp = &p;            // 存 p 的地址\n    **pp = 99;                // 通过二级指针改 a\n    printf(\"a = %d\\n\", a);   // 99\n\n    // 4. 空指针与野指针\n    int* wild;                // 野指针：未初始化，禁止解引用\n    int* safe = nullptr;      // 空指针\n    if (safe != nullptr) *safe = 1;  // 先判空再用\n\n    // 5. void* 与动态内存\n    void* raw = malloc(sizeof(int));\n    int* ip = (int*)raw;      // 必须转换后才能解引用\n    *ip = 42;\n    printf(\"*ip = %d\\n\", *ip);\n    free(ip);\n    ip = nullptr;             // 释放后置空，防止悬空野指针\n    return 0;\n}\n```\n\n二级指针最典型的应用——在函数内修改外部指针：\n\n```C++\nvoid allocInt(int** out, int v) {   // 传指针的地址\n    *out = (int*)malloc(sizeof(int));\n    **out = v;\n}\n// 调用：int* p = nullptr; allocInt(&p, 5);\n// 若只传 int*，函数里改的只是拷贝，外面的 p 不会变\n```\n\n## 面试常问\n\n**Q1：指针和数组名有什么区别？**\n数组名是地址常量，不能自增或重新赋值；`sizeof(数组名)` 得到整个数组大小，`sizeof(指针)` 得到指针大小；数组作为函数参数时会退化为指针。\n\n**Q2：什么是野指针？如何避免？**\n指向“不确定内容”的指针：未初始化、或所指内存已释放/失效。避免：定义即初始化、`free`/`delete` 后置 `nullptr`、不返回局部变量地址。\n\n**Q3：NULL 和 nullptr 有什么区别？**\n`NULL` 在 C++ 中本质是整数 0，传参重载时可能产生二义性；`nullptr` 是类型安全的空指针常量，C++11 起推荐使用。\n\n**Q4：为什么需要二级指针？**\nC 是值传递，想在函数内修改外部的“指针本身”（而不是它指向的值），必须传指针的地址，即二级指针。\n\n**Q5：void* 指针能直接解引用或做加减运算吗？**\n不能。`void*` 不知道所指向数据的大小和类型，必须先强制转换为具体类型指针后才能解引用或做指针运算。','generate://c-pointer','cpp','generated',0,'2026-09-03 21:08:59','2026-09-03 21:08:59',0,NULL,NULL),(262,'C 数组与字符串','# C 数组与字符串\n\n## 是什么\n\n数组就像一排编了号的储物柜：柜子从 **0** 开始编号，每个柜子大小相同、装同一种类型的数据。一维数组是一排柜子，二维数组则是柜子排成的“表格”。\n\n字符串在 C 里没有独立类型，本质就是一个 char 数组，只是约定在末尾放一个特殊字符 `\'\\0\'`（空字符）表示“到此结束”。\n\n用术语说：数组是**同类型元素在内存中连续存放**的块，通过下标以 O(1) 速度访问任意元素；C 字符串是**以 `\'\\0\'` 结尾的字符数组**。\n\n## 为什么重要\n\n- 数组是链表、栈、哈希表等几乎所有数据结构的底层存储基础。\n- 文本处理、文件读写、网络协议全靠字符串，而 C 字符串操作全靠 char 数组 + string.h 函数。\n- 数组直接暴露内存布局，学懂它才能理解指针、函数传参、越界这些 C 的核心难点。\n- 笔试面试高频考点：`sizeof` vs `strlen`、缓冲区溢出、二维数组存储方式等。\n\n## 核心用法\n\n**1. 一维数组**：`int a[5] = {1, 2, 3};` 定义 5 个 int，初始化列表不够的元素自动补 0（局部数组完全不初始化则内容是随机值）。下标范围 0~4，数组长度 = `sizeof(a) / sizeof(a[0])`。\n\n**2. 二维数组**：`int m[3][4]` 表示 3 行 4 列共 12 个元素，内存中按**行优先**连续存放。访问 `m[i][j]`，i 最大到 2，j 最大到 3。\n\n**3. char 数组与 \'\\0\'**：`char s[] = \"hi\";` 实际占 3 字节：`\'h\'`、`\'i\'`、`\'\\0\'`。printf 的 `%s`、strlen 等都靠 `\'\\0\'` 判断结尾——丢了它，程序会越过数组末尾继续读内存，导致乱码甚至崩溃。\n\n**4. string.h 常用函数**：\n\n| 函数 | 作用 |\n|---|---|\n| `strlen(s)` | 求实际长度（不含 \'\\0\'） |\n| `strcpy(dst, src)` / `strncpy(dst, src, n)` | 复制 / 复制最多 n 字节 |\n| `strcat(dst, src)` / `strncat` | 拼接 |\n| `strcmp(s1, s2)` / `strncmp` | 比较，相等返回 0 |\n| `strchr(s, c)` | 查找字符首次出现位置 |\n\n注意：这些函数**不检查目标数组够不够大**，空间要程序员自己保证。\n\n**5. 越界风险**：C/C++ 编译器和运行时都**不检查**下标是否越界，越界属于**未定义行为（UB）**——可能静默改写别的变量、可能崩溃、也可能“看起来正常”从而埋雷。`strcpy` 把长字符串拷进短数组就是典型越界。防御手段：循环条件写 `i < n` 而非 `i <= n`、复制用 `strncpy` 限长并手动补 `\'\\0\'`、用 `sizeof` 计算数组长度。\n\n## 代码示例\n\n```C++\n#include <cstdio>\n#include <cstring>\n\nint main() {\n    // 1. 一维数组：求和\n    int a[5] = {1, 2, 3, 4, 5};\n    int sum = 0;\n    for (int i = 0; i < 5; ++i)      // 注意是 i < 5，不是 i <= 5\n        sum += a[i];\n    printf(\"sum=%d\\n\", sum);         // 15\n\n    // 2. 二维数组：矩阵\n    int m[2][3] = {{1, 2, 3}, {4, 5, 6}};\n    printf(\"m[1][2]=%d\\n\", m[1][2]); // 6\n\n    // 3. char 数组与 \'\\0\'\n    char s1[] = \"hi\";                 // 实际是 {\'h\',\'i\',\'\\0\'}\n    printf(\"sizeof=%zu strlen=%zu\\n\",\n           sizeof(s1), strlen(s1));   // 3  2\n\n    // 4. string.h 函数\n    char buf[16];\n    strcpy(buf, \"hello\");             // 复制（目标必须够大）\n    strcat(buf, \" world\");            // 拼接：11 字符 + \'\\0\' = 12 字节\n    if (strcmp(buf, \"hello world\") == 0)\n        printf(\"equal: %s\\n\", buf);\n\n    char small[4];\n    strncpy(small, \"abcdefg\", sizeof(small) - 1);  // 限长，防止越界\n    small[sizeof(small) - 1] = \'\\0\';               // strncpy 不补 \'\\0\'，需手动补\n    printf(\"small=%s\\n\", small);                   // abc\n\n    // 5. 越界写法（错误示范，千万别学）\n    // a[5] = 99;   // 编译不报错，但越界写是未定义行为！\n    return 0;\n}\n```\n\n## 面试常问\n\n**Q1：strlen 和 sizeof 有什么区别？**\nsizeof 是运算符，返回整个数组占的字节数（含 `\'\\0\'`）；strlen 是函数，数到 `\'\\0\'` 为止的实际字符数（不含 `\'\\0\'`），且要求参数必须以 `\'\\0\'` 结尾。\n\n**Q2：`\'\\0\'` 是什么？没有它会怎样？**\n值为 0 的空字符，是 C 字符串结束标志；没有它，strlen、printf 等会越过数组继续读内存（未定义行为），典型表现是乱码或崩溃。\n\n**Q3：为什么 strcpy 不安全？**\n它不检查目标空间大小，源串过长就会越界写入造成缓冲区溢出；安全做法是用 strncpy 限长并手动补 `\'\\0\'`，或确保目标足够大。\n\n**Q4：C 语言会检查数组越界吗？**\n不会。越界读写是未定义行为，编译和运行时都可能毫无提示，必须由程序员保证下标在 `[0, n-1]` 内。\n\n**Q5：二维数组 `int m[3][4]` 在内存中如何存放？**\n按行优先连续存放 12 个 int；元素 `m[i][j]` 的地址等价于 `&m[0][0] + i * 4 + j`。','generate://c-array-string','cpp','generated',0,'2026-09-03 21:10:03','2026-09-03 21:10:03',0,NULL,NULL),(263,'C 函数与结构体','# C 函数与结构体\n\n## 是什么\n\n大白话版本：\n\n- **函数**就是一台“榨汁机”：你把原料（参数）放进去，它加工后吐出一杯果汁（返回值）。原料放进去后，机器内部怎么折腾你不用管。\n- **结构体**就是一张“个人档案卡”：姓名、年龄、身高这些不同类型的信息，本来散落一地，结构体把它们装订成一张卡，拎起来就能整体带走。\n\n术语版本：函数是具有独立逻辑、可被反复调用的代码单元，通过参数接收输入、返回值输出结果；结构体是用户自定义的聚合数据类型，将多个不同类型的成员变量组合为一个逻辑整体。\n\n## 为什么重要\n\n1. 没有函数，同一段逻辑要到处复制粘贴；有了函数，写一次、处处调用，这是代码复用的最小单元。\n2. 只靠 `int`、`char` 这些零散的基本类型描述“一个学生”，数据之间毫无关联；结构体让数据有了“归属感”，是面向对象思想在 C 中的雏形。\n3. **值传递与指针传递是理解 C 内存模型的分水岭**，搞不懂它就永远写不对“让函数修改外部变量”的代码。\n4. 函数指针是回调机制、`qsort`、状态机表驱动设计的基石，是从“会写代码”迈向“会设计代码”的门票。\n\n## 核心用法\n\n### 1. 值传递 vs 指针传递\n\n- 值传递：函数拿到的是实参的**复印件**，改复印件不影响原件。\n- 指针传递：函数拿到的是**原件的存放地址**，顺着地址就能改原件本身。\n- 何时用指针：① 需要在函数内修改外部变量；② 结构体很大，拷贝一份太浪费；③ 想返回多个结果。\n- 加 `const` 修饰指针参数，可以“只读不改”，既高效又安全。\n\n### 2. 结构体的定义与访问\n\n```C++\nstruct Student {\n    char name[20];\n    int  age;\n    float score;\n};\n\nstruct Student s1 = {\"Tom\", 18, 92.5};\ns1.age = 19;                    // 变量用 . 访问\n\nstruct Student *p = &s1;\np->age = 20;                    // 指针用 -> 访问，等价于 (*p).age\n```\n\n### 3. typedef struct\n\n给结构体类型起个别名，之后声明变量不用再写 `struct` 关键字，代码更清爽：\n\n```C++\ntypedef struct {\n    int x;\n    int y;\n} Point;\n\nPoint p = {3, 4};   // 不用写 struct Point\n```\n\n### 4. 函数指针入门\n\n函数名本质上就是函数代码的入口地址。把地址存进变量，就能“换个函数来执行”：\n\n```C++\nint add(int a, int b) { return a + b; }\n\nint (*fp)(int, int) = add;   // 声明并指向 add\nfp(1, 2);                    // 等价于 add(1, 2)\n```\n\n读法：`(*fp)` 表示 fp 是指针，`(int, int)` 和前面的 `int` 是它所指函数的参数与返回值。\n\n## 代码示例\n\n```C++\n#include <stdio.h>\n\ntypedef struct {\n    char name[20];\n    int  age;\n    float score;\n} Student;\n\n// 值传递：只改副本，外部不受影响\nvoid tryGrowUp(Student s) {\n    s.age += 1;\n    printf(\"[值传递] 函数内 age = %d\\n\", s.age);\n}\n\n// 指针传递：通过地址修改原结构体\nvoid growUp(Student *s) {\n    s->age += 1;\n}\n\n// 函数指针作参数：实现\"回调\"\nvoid apply(Student *s, void (*op)(Student *)) {\n    op(s);\n}\n\nint main() {\n    Student s = {\"Tom\", 18, 92.5};\n\n    tryGrowUp(s);\n    printf(\"值传递后 age = %d\\n\", s.age);   // 18，没变！\n\n    growUp(&s);\n    printf(\"指针传递后 age = %d\\n\", s.age); // 19，改了\n\n    apply(&s, growUp);\n    printf(\"回调后 age = %d\\n\", s.age);     // 20\n\n    return 0;\n}\n```\n\n## 面试常问\n\n**Q1：值传递和指针传递有什么区别？**\n值传递拷贝实参副本，函数内修改不影响外部；指针传递的是地址，通过解引用可直接修改原变量。大结构体传参优先用指针以避免拷贝开销。\n\n**Q2：结构体中 `.` 和 `->` 的区别？**\n`.` 用于结构体变量访问成员，`->` 用于结构体指针访问成员，`p->age` 完全等价于 `(*p).age`。\n\n**Q3：`typedef struct` 和直接 `struct` 有什么区别？**\n`typedef` 给结构体类型起了别名，后续声明变量不用再写 `struct` 关键字，代码更简洁，也常用于实现类型的平台无关抽象。\n\n**Q4：什么是函数指针？典型用途是什么？**\n指向函数入口地址的指针，声明形如 `int (*fp)(int, int);`，典型用途包括回调函数、`qsort` 的比较器、表驱动设计（用函数指针数组替代大量 if-else）。\n\n**Q5：结构体传参为什么推荐传指针并加 const？**\n指针传递避免整块拷贝的性能开销；若函数不修改数据，加 `const Student *s` 既能向调用者承诺“只读”，又能在编译期拦截误改。','generate://c-func-struct','cpp','generated',0,'2026-09-03 21:10:50','2026-09-03 21:10:50',0,NULL,NULL),(264,'C 内存管理：malloc / free','# C 内存管理：malloc / free\n\n## 是什么\n\n大白话：程序运行时要存数据，**栈**就像办公桌上随手放东西的位置，函数一结束就被自动收走；`malloc` 则是向系统租一间仓库——多大、什么时候还，全由你说了算，`free` 就是退租。\n\n术语：`malloc` / `calloc` / `realloc` / `free` 是 C 标准库 `<stdlib.h>` 中管理**堆内存**的四个函数。`malloc` 在堆上分配指定字节数的连续内存并返回 `void*` 指针；`free` 将其归还系统。与编译器自动管理的栈不同，堆内存的申请与释放完全由程序员手动控制。\n\n## 为什么重要\n\n1. 栈空间小（一般 1~8 MB）且大小编译期就定死了。当需要「运行时才知道多大」的内存（如按用户输入建的数组），或需要数据在函数返回后继续存活（如链表节点），就必须用堆。\n2. 堆是 C 程序 bug 的重灾区：内存泄漏、悬垂指针、越界写都源于手动管理出错。熟练掌握 malloc/free 是 C 程序员的基本功，也是面试高频考点。\n\n## 核心用法\n\n**栈 vs 堆速查表**\n\n| 对比项 | 栈 | 堆 |\n|---|---|---|\n| 分配/释放 | 自动，函数返回即回收 | 手动，malloc/free |\n| 大小 | 小（MB 级） | 大（受限于物理内存） |\n| 速度 | 快 | 相对慢 |\n| 生命周期 | 与作用域绑定 | 由程序员控制 |\n\n**四个函数**\n\n- `malloc(size)`：分配 size 字节，**内容未初始化**（是垃圾值）；失败返回 `NULL`，**必须检查**。\n- `calloc(n, size)`：分配 n×size 字节，**自动清零**。\n- `realloc(ptr, new_size)`：调整已有内存大小，可能原地扩展，也可能搬家并拷贝数据，返回新地址。\n- `free(ptr)`：释放 ptr 指向的内存；只能释放 malloc/calloc/realloc 返回的指针，同一块内存不能 free 两次；`free(NULL)` 是安全的。\n\n**三条铁律**\n\n1. malloc 返回值必须判空：`if (p == NULL)`。\n2. 谁申请谁释放，杜绝内存泄漏（申请了不还 = 泄漏）。\n3. free 之后立刻 `p = NULL;`，防止悬垂指针被误用。\n\n## 代码示例\n\n```C++\n#include <stdio.h>\n#include <stdlib.h>\n\nint main(void) {\n    int n = 10;\n\n    /* 1. malloc：申请 n 个 int，必须检查返回值 */\n    int *arr = (int *)malloc(n * sizeof(int));\n    if (arr == NULL) {\n        fprintf(stderr, \"malloc failed\\n\");\n        return 1;\n    }\n    for (int i = 0; i < n; i++) arr[i] = i * 2;\n\n    /* 2. realloc：扩容到 20 个 int，用临时指针接收防泄漏 */\n    int *tmp = (int *)realloc(arr, 20 * sizeof(int));\n    if (tmp == NULL) {\n        free(arr);          /* 原内存仍有效，退出前先释放 */\n        return 1;\n    }\n    arr = tmp;\n    arr[19] = 999;\n    printf(\"arr[0]=%d, arr[19]=%d\\n\", arr[0], arr[19]);\n\n    /* 3. free：释放并置空，防止悬垂指针 */\n    free(arr);\n    arr = NULL;\n\n    /* 4. calloc：分配并清零 */\n    int *z = (int *)calloc(5, sizeof(int));\n    if (z == NULL) return 1;\n    printf(\"z[0]=%d\\n\", z[0]);   /* 一定是 0 */\n    free(z);\n    z = NULL;\n    return 0;\n}\n```\n\n反例（千万别学）：\n\n```C++\nint *p = (int *)malloc(sizeof(int));\nfree(p);     /* 内存已归还系统 */\n*p = 42;     /* 悬垂指针：use-after-free，未定义行为！ */\n```\n\n注意：若在 free 之前提前 `return`，这块堆内存就再也找不回来——这就是内存泄漏。\n\n## 面试常问\n\n**Q1：栈和堆有什么区别？**\n栈由编译器自动分配释放，小而快，随函数返回回收；堆通过 malloc/free 手动管理，大但慢，生命周期由程序员控制。\n\n**Q2：malloc 和 calloc 的区别？**\ncalloc 参数为 (元素个数， 单个大小) 且把内存清零；malloc 参数为总字节数，内容是未初始化的垃圾值。\n\n**Q3：什么是内存泄漏？如何检测？**\n申请的堆内存不再使用却没有 free，导致长期运行的进程内存持续增长；可用 valgrind 或 `-fsanitize=address` 检测。\n\n**Q4：什么是悬垂指针？如何避免？**\nfree 后仍指向旧地址的指针，解引用属未定义行为；free 后立即置 NULL，使用前判空。\n\n**Q5：为什么必须检查 malloc 返回值？realloc 失败怎么办？**\n内存不足时 malloc 返回 NULL，不判空直接使用会崩溃；realloc 失败返回 NULL 但原内存有效，应用临时变量接收、成功后再覆盖原指针。','generate://c-memory','cpp','generated',0,'2026-09-03 21:12:22','2026-09-03 21:12:22',0,NULL,NULL),(265,'C 编译链接与预处理','# C 编译链接与预处理\n\n## 是什么\n\n大白话：你写的 `.c` 文件是给人看的，机器看不懂。编译就是把“人话”一步步翻译成“机器话”的流水线，共四站：**预处理 → 编译 → 汇编 → 链接**。\n\n术语上：预处理负责处理所有以 `#` 开头的指令（`#define`、`#include` 等）；编译把预处理结果翻译成汇编代码；汇编再翻译成机器码（目标文件 `.o`）；链接把多个 `.o` 和库拼装成最终可执行文件。\n\n## 为什么重要\n\n- **看得懂报错**：`undefined reference` 是链接错误不是语法错误，不懂链接就无从下手。\n- **多文件工程的基本功**：声明与定义分离是所有 C 项目的组织方式。\n- **经典坑的根源**：重复包含头文件、重复定义变量，问题都出在预处理和链接环节。\n- **高频面试考点**，几乎必问。\n\n## 核心用法\n\n**1. gcc 编译四阶段**\n\n| 阶段 | 输入 → 输出 | 干什么 |\n|------|------------|--------|\n| 预处理 | .c → .i | 展开 `#include`、替换 `#define`、删除注释 |\n| 编译 | .i → .s | 翻译成汇编代码，做语法和语义检查 |\n| 汇编 | .s → .o | 翻译成机器码目标文件 |\n| 链接 | .o(+库) → 可执行 | 符号解析 + 重定位 |\n\n**2. #define：纯文本替换**\n\n- 没有类型、没有计算，只是把 `MAX_LEN` 替换成 `100`；末尾**不加**分号。\n- 宏函数必须给参数和整体加括号，`#define SQUARE(x) ((x)*(x))`，否则 `SQUARE(1+2)` 会错。\n\n**3. #include：复制粘贴**\n\n`<xxx.h>` 只在系统目录找；`\"xxx.h\"` 先在当前目录找，找不到再找系统目录。本质是把头文件内容原样插入当前文件。\n\n**4. 头文件守卫**\n\n防止同一头文件被重复包含（复制粘贴两遍），造成类型、变量重复定义。\n\n**5. 声明与定义分离**\n\n- **定义**：创建实体、分配内存，如 `int g = 0;`、带函数体的函数。\n- **声明**：只告诉编译器“这东西存在”，如 `extern int g;`、函数原型。\n- **单一定义规则（ODR）**：一个符号全程序只能定义一次，可声明多次。因此头文件放声明，源文件放定义。\n\n## 代码示例\n\n```C++\n// gcc -E main.c -o main.i   预处理\n// gcc -S main.i -o main.s   编译\n// gcc -c main.s -o main.o   汇编\n// gcc main.o math_utils.o -o app   链接（多个源文件一起编）\n\n// ---------- math_utils.h ----------\n#ifndef MATH_UTILS_H          // 头文件守卫：第二次包含时宏已定义，整段跳过\n#define MATH_UTILS_H\n\n#define MAX_LEN 100               // 宏常量\n#define SQUARE(x) ((x) * (x))     // 宏函数：括号防坑\n\nint add(int a, int b);            // 函数声明：只有原型，没有函数体\nextern int g_count;               // 变量声明：extern 表示\"定义在别的文件里\"\n\n#endif // MATH_UTILS_H\n```\n\n```C++\n// ---------- math_utils.c ----------\n#include \"math_utils.h\"\n\nint g_count = 0;               // 变量定义：分配内存，全项目仅此一处\n\nint add(int a, int b)         // 函数定义：带函数体\n{\n    ++g_count;\n    return a + b;\n}\n```\n\n```C++\n// ---------- main.c ----------\n#include <stdio.h>\n#include \"math_utils.h\"       // 有了声明，编译器知道 add 和 g_count 长什么样\n\nint main(void)\n{\n    printf(\"%d\\n\", add(MAX_LEN, SQUARE(3)));  // 预处理后变成 add(100, ((3)*(3)))\n    printf(\"count=%d\\n\", g_count);            // 编译时只留地址占位，链接时去 math_utils.o 中找\n    return 0;\n}\n```\n\n## 面试常问\n\n**Q1：gcc 编译的四个阶段？**\n预处理（处理 # 指令得 .i）→ 编译（得汇编 .s）→ 汇编（得目标文件 .o）→ 链接（合并目标文件和库生成可执行文件）。\n\n**Q2：`#include <>` 和 `\"\"` 的区别？**\n`<>` 只查系统头文件目录，用于标准库；`\"\"` 先查源文件所在目录再查系统目录，用于自定义头文件。\n\n**Q3：头文件守卫的作用？`#pragma once` 呢？**\n防止头文件被重复包含导致重复定义；`#ifndef/#define/#endif` 是标准写法，`#pragma once` 更简洁但是编译器扩展，非 C 标准。\n\n**Q4：声明和定义的区别？头文件为什么放声明不放定义？**\n声明不分配内存、可多次出现；定义分配内存、全程序仅一次（ODR）。头文件会被多处包含，若放定义必然引发重复定义的链接错误。\n\n**Q5：`undefined reference` 和 `multiple definition` 是什么阶段的问题？**\n都是链接错误：前者是声明了但链接器找不到定义（漏链 .o、漏链库）；后者是同一符号被定义了多次（如头文件里定义了变量）。','generate://c-compile','cpp','generated',0,'2026-09-03 21:13:27','2026-09-03 21:13:27',0,NULL,NULL),(266,'C++ 类与对象：构造 / 析构','# C++ 类与对象：构造 / 析构\n\n## 是什么\n\n大白话：**类是图纸，对象是按图纸造出来的产品**。构造函数是“出厂组装流程”，析构函数是“报废回收流程”，两者都由编译器自动调用，你不用手动喊。\n\n术语：`class` 是用户自定义类型，把数据（成员变量）和操作（成员函数）封装在一起；访问控制（`public` / `private` / `protected`）决定外部能访问什么。**构造函数**与类同名、无返回值，对象诞生时自动执行；**析构函数**写作 `~类名`，对象生命周期结束时自动执行，用于释放资源。**拷贝构造函数**规定“用一个旧对象造一个新对象”时该怎么复制。\n\n## 为什么重要\n\n1. 不写构造函数，内置类型的成员变量是**未初始化的垃圾值**，程序行为不可预测。\n2. C++ 没有 GC，`new` 出来的资源要靠析构函数配对释放，配不上就**内存泄漏**。\n3. 默认的拷贝是逐成员浅拷贝，类里有裸指针时会**双重释放**，直接崩溃。\n\n## 核心用法\n\n**1. 访问控制**：`class` 默认 `private`。惯例：数据 `private`（隐藏细节），接口 `public`。单参数构造建议加 `explicit`，防止隐式类型转换。\n\n**2. 构造函数与初始化列表**：构造函数可重载。初始化列表写法 `X(int a) : x_(a) {}`，在函数体执行**之前**完成初始化。三类成员必须用初始化列表：`const` 成员、引用成员、没有默认构造的成员类型。初始化顺序按**成员声明顺序**，与列表书写顺序无关。\n\n**3. 析构时机**：局部对象离开作用域时析构；`new` 的对象在 `delete` 时析构；全局/静态对象在 `main` 结束后析构；临时对象在所在语句结束时析构。总体规则：**析构顺序与构造相反**。作为多态基类的类，析构函数要声明为 `virtual`。\n\n**4. 拷贝构造**：签名 `X(const X& other)`。三种触发场景：用已有对象初始化新对象、按值传参、按值返回。含裸指针时必须自己写**深拷贝**（重新分配内存并复制内容），否则默认浅拷贝让两个对象共享同一块内存，析构时 delete 两次。\n\n## 代码示例\n\n```C++\n#include <iostream>\n#include <cstring>\n\nclass Buffer {\nprivate:                       // 数据私有，隐藏实现\n    char*  data_;\n    size_t size_;\n\npublic:\n    // 构造函数 + 初始化列表\n    explicit Buffer(size_t n) : data_(new char[n]{}), size_(n) {\n        std::cout << \"构造\\n\";\n    }\n\n    // 拷贝构造：深拷贝，各管各的内存\n    Buffer(const Buffer& other)\n        : data_(new char[other.size_]), size_(other.size_) {\n        std::memcpy(data_, other.data_, size_);\n        std::cout << \"拷贝构造\\n\";\n    }\n\n    ~Buffer() {                // 析构：释放资源\n        delete[] data_;\n        std::cout << \"析构\\n\";\n    }\n\n    size_t size() const { return size_; }   // public 接口\n};\n\nint main() {\n    Buffer a(8);        // 普通构造\n    Buffer b = a;        // 拷贝构造（深拷贝）\n\n    {\n        Buffer c(4);\n    }                     // c 在此析构，先于 a、b（后构造先析构）\n\n    return 0;\n}                         // main 结束：b、a 依次析构\n```\n\n若删掉自定义拷贝构造，`a` 和 `b` 的 `data_` 指向同一块内存，程序结束时 delete 两次，直接崩溃。\n\n## 面试常问\n\n**Q1：为什么拷贝构造函数的参数必须是引用？**\n若是按值传参，传参这一步本身又要调用拷贝构造，形成无限递归，编译直接报错。\n\n**Q2：浅拷贝和深拷贝的区别？**\n浅拷贝只复制指针的值，两对象共享资源，析构时重复释放；深拷贝重新分配内存并复制内容，各自独立。\n\n**Q3：什么时候必须用初始化列表？**\n`const` 成员、引用成员、没有默认构造的成员类型；此外对已有复杂成员，列表初始化可省去“先默认构造再赋值”的开销。\n\n**Q4：构造函数可以是虚函数吗？析构函数呢？**\n构造函数不行——虚函数依赖对象的 vptr，而 vptr 要在构造过程中才建立；基类析构函数通常应声明为 `virtual`，保证通过基类指针 `delete` 派生类对象时派生部分被正确析构。\n\n**Q5：构造和析构的顺序规则是什么？**\n单个对象内：成员按声明顺序构造，按声明**逆序**析构；继承体系：先基类后派生类构造，析构相反；同一作用域的多个局部对象：先构造的后析构（栈结构）。','generate://cpp-class','cpp','generated',0,'2026-09-03 21:14:15','2026-09-03 21:14:15',0,NULL,NULL),(267,'C++ 引用与 const','# C++ 引用与 const\n\n## 是什么\n\n**大白话**：引用就是给变量起“外号”——叫外号和叫本名，指的都是同一个人；`const` 就是一把“只读锁”——被锁住的东西只能看，不能改。\n\n**术语**：引用（reference）是已存在变量的别名，定义时必须绑定某个变量，之后对引用的一切读写都直接作用于原变量，底层通常由编译器用常量指针实现。`const` 是类型修饰符，用于声明只读语义，任何试图通过 const 名字修改对象的代码都会在编译期报错。\n\n## 为什么重要\n\n1. **省拷贝**：传一个大对象（长字符串、大结构体），值传递要完整复制一份；传引用只传“别名”，成本近乎为零。\n2. **保安全**：`const` 让编译器帮你站岗——谁想改只读数据，直接编译报错，把运行期 bug 扼杀在编译期。\n3. **面试硬通货**：引用与指针的区别、`const` 位置辨析、引用返回的陷阱，几乎是 C++ 面试的必考题。\n\n## 核心用法\n\n### 1. 引用 vs 指针\n\n| 维度 | 引用 | 指针 |\n|---|---|---|\n| 初始化 | 必须初始化 | 可以不初始化 |\n| 能否为空 | 不能 | 可为 `nullptr` |\n| 换绑定目标 | 不能，从一而终 | 可随时改指向 |\n| 多级嵌套 | 没有\"引用的引用\" | 有二级、三级指针 |\n\n一句话：引用是“别名”，指针是“地址”，引用更安全，指针更灵活。\n\n### 2. const 指针 与 指向 const 的指针\n\n- `const int* p`：**指向 const 的指针**——所指内容只读，指针本身可换指向。\n- `int* const p`：**const 指针**——指针指向固定，所指内容可改。\n- 口诀：`const` 在 `*` 左边管“内容”，在 `*` 右边管“指针”。\n\n### 3. const 引用传参\n\n`void f(const BigObj& obj)` 是传参的黄金写法：`&` 避免拷贝（快），`const` 保证函数不改实参（安全）。额外福利：const 引用还能绑定临时值，如 `const int& r = 42;` 合法。\n\n### 4. 引用返回\n\n返回引用可避免拷贝返回值，常用于 `operator[]`、链式调用，且返回的引用可以当左值用。**铁律**：绝不能返回局部变量的引用——函数一结束局部变量就销毁，引用悬垂，后续使用是未定义行为。安全做法：返回类成员、静态变量或参数的引用。\n\n## 代码示例\n\n```C++\n#include <iostream>\n#include <string>\n\nint main() {\n    // 1. 引用：变量的别名\n    int a = 10;\n    int& ra = a;          // ra 是 a 的别名\n    ra = 20;              // 改 ra 就是改 a\n    std::cout << a << \"\\n\";   // 输出 20\n\n    // 2. const 指针 vs 指向 const 的指针\n    int x = 1, y = 2;\n    const int* p1 = &x;   // 指向 const 的指针：内容只读\n    // *p1 = 9;            // ✗ 编译报错\n    p1 = &y;              // ✓ 换指向合法\n\n    int* const p2 = &x;   // const 指针：指针本身只读\n    // p2 = &y;           // ✗ 编译报错\n    *p2 = 100;            // ✓ 改内容合法\n\n    return 0;\n}\n```\n\n```C++\n#include <iostream>\n#include <string>\n\n// 3. const 引用传参：不拷贝、不改、能接临时值\nvoid printMsg(const std::string& msg) {\n    // msg = \"hack\";      // ✗ 编译报错，const 保护实参\n    std::cout << msg << \"\\n\";\n}\n\n// 4. 引用返回：返回成员的引用，生命周期由对象保证，安全\nclass Array {\n    int data[10] = {0};\npublic:\n    int& at(int i) { return data[i]; }\n};\n\n// 危险示范：返回局部变量的引用 = 悬垂引用\nint& bad() {\n    int local = 42;\n    return local;        // ✗ 未定义行为\n}\n\nint main() {\n    printMsg(\"hello\");   // 临时 string 直接绑定 const 引用\n\n    Array arr;\n    arr.at(3) = 7;       // 返回引用可当左值\n    std::cout << arr.at(3) << \"\\n\";   // 输出 7\n}\n```\n\n## 面试常问\n\n**Q1：引用和指针有什么区别？**\n引用必须初始化、不能为空、不能换绑、没有多级引用；指针全都相反。引用更安全，指针更灵活。\n\n**Q2：`const int* p` 和 `int* const p` 有何区别？**\n前者指向的内容不可改（指向 const 的指针），后者指针本身不可改（const 指针）；看 `const` 在 `*` 左边还是右边。\n\n**Q3：函数参数为什么推荐 `const T&`？**\n引用避免值拷贝提升性能，`const` 保证函数不改实参，且能接受临时值作为实参。\n\n**Q4：可以返回局部变量的引用吗？**\n不可以。局部变量出作用域即销毁，产生悬垂引用，属未定义行为；应返回成员、静态变量或参数的引用。\n\n**Q5：const 引用能绑定临时值吗？**\n可以，如 `const int& r = 42;` 且会延长临时值生命周期；非 const 的左值引用 `int& r = 42;` 则编译报错。','generate://cpp-ref-const','cpp','generated',0,'2026-09-03 21:15:14','2026-09-03 21:15:14',0,NULL,NULL),(268,'C++ 继承与多态：虚函数','# C++ 继承与多态：虚函数\n\n## 是什么\n\n大白话：继承让“儿子类”自动拥有“父亲类”的功能；多态让同一句调用“看人下菜碟”——对象是谁，就执行谁的行为。虚函数（`virtual`）就是打开这个开关的关键字。\n\n术语：在基类成员函数前加 `virtual`，该函数即进入**动态绑定**机制——通过基类指针或引用调用时，程序在**运行期**根据对象实际类型选择执行哪个版本。编译器为每个含虚函数的类生成一张**虚函数表（vtable）**，对象内部藏一个指针（vptr）指向它。\n\n## 为什么重要\n\n- 没有虚函数就没有运行期多态，面向对象三大特性就缺一角。\n- 框架和库大量用虚函数做接口与扩展点（回调、插件、策略模式）。\n- 不懂 vtable，就无法理解虚析构的必要性、虚调用的开销、对象的内存布局。\n- 面试 C++ 岗的高频必考题。\n\n## 核心用法\n\n1. **虚函数**：基类函数加 `virtual`，派生类用相同签名的函数覆盖，建议加 `override` 让编译器帮你查错。\n2. **派生类构造顺序**：先调基类构造 → 再按声明顺序构造派生类自己的成员 → 最后执行派生类构造函数体；析构顺序完全相反。注意：构造/析构期间虚函数**不发生多态**，按当前正在构造的那个类解析。\n3. **虚函数表**：vtable 每个类一份（编译期生成，常放只读数据段），对象里只存一个 vptr。调用过程：对象 → vptr → vtable 中第 n 项 → 真正的函数地址，开销很小但非零。\n4. **纯虚函数**：写成 `virtual void f() = 0;`，只定义接口不给默认实现，使类成为**抽象类**——不能创建对象，只能当基类；派生类必须实现全部纯虚函数才能实例化。\n5. **虚析构**：用基类指针 `delete` 派生类对象时，若基类析构不是 `virtual`，通常只执行基类析构，派生类那部分资源泄漏（标准层面是未定义行为）。**凡是会被继承且可能通过基类指针删除的类，析构函数必须为虚。**\n\n## 代码示例\n\n```C++\n#include <iostream>\n#include <memory>\n\nclass Animal {                                   // 抽象类：含纯虚函数\npublic:\n    Animal()  { std::cout << \"1. 基类构造\\n\"; }\n    virtual ~Animal() { std::cout << \"4. 基类析构\\n\"; }  // 虚析构：必要！\n    virtual void speak() const = 0;              // 纯虚函数：只定接口\n};\n\nclass Dog : public Animal {\npublic:\n    Dog()  { std::cout << \"2. 派生类构造\\n\"; }\n    ~Dog() override { std::cout << \"3. 派生类析构\\n\"; }\n    void speak() const override { std::cout << \"汪汪\\n\"; }\n};\n\nint main() {\n    // Animal a;    // 编译错误：抽象类不能实例化，但可定义指针/引用\n    std::unique_ptr<Animal> a = std::make_unique<Dog>();\n    // 输出: 1. 基类构造 -> 2. 派生类构造（先基类，后派生类）\n\n    a->speak();     // 汪汪：运行期经 vptr 查虚表，定位到 Dog::speak\n\n    return 0;\n}   // 作用域结束: 3. 派生类析构 -> 4. 基类析构（顺序相反；若无虚析构，\n    // 经 Animal* 删除时通常只执行 4，Dog 的资源就泄漏了）\n```\n\n## 面试常问\n\n**Q1：构造函数能是虚函数吗？**\n不能。虚调用依赖对象里的 vptr，而 vptr 恰恰要在构造过程中初始化，鸡生蛋问题；且构造时类型明确，无需多态。\n\n**Q2：派生类对象的构造和析构顺序是什么？**\n构造：基类构造 → 派生类成员构造 → 派生类构造函数体；析构完全相反：派生类析构 → 派生类成员析构 → 基类析构。\n\n**Q3：为什么基类析构函数必须声明为 virtual？**\n基类指针 `delete` 派生对象时，非虚析构只调基类析构，派生类资源泄漏（属未定义行为）；虚析构保证派生类部分被正确析构。\n\n**Q4：虚函数表存在哪里？每个对象一份吗？**\nvtable 每个类一份，编译期生成、常在只读数据段；对象只存一个 vptr 指向它，所以含虚函数的类对象会变大（通常多 8 字节）。\n\n**Q5：抽象类和纯虚函数的关系？**\n类中只要有一个 `= 0` 的纯虚函数，该类即抽象类，不能实例化，只能做基类；派生类必须实现所有纯虚函数才能创建对象。','generate://cpp-inherit-poly','cpp','generated',0,'2026-09-03 21:16:21','2026-09-03 21:16:21',0,NULL,NULL),(269,'C++ STL 常用容器与算法','# C++ STL 常用容器与算法\n\n## 是什么\n\nSTL（Standard Template Library，标准模板库）是 C++ 标准库自带的“工具箱”。大白话说：以前存数据、查数据、排序都得自己手写，STL 把这些高频工具提前造好了，拿来就用。\n\n它包含三大件：\n- **容器**：装数据的“盒子”，如 `vector`（动态数组）、`string`（字符串）、`map`（映射/字典）；\n- **迭代器**：类似“指针”的定位器，指向容器中的元素，负责遍历；\n- **算法**：现成函数库，如 `sort` 排序、`find` 查找、`reverse` 翻转。\n\n## 为什么重要\n\n1. **省时省力**：`sort(v.begin(), v.end())` 一行代码顶手写几十行快排；\n2. **稳定可靠**：工业级实现、久经考验，边界处理比自己写的安全；\n3. **笔试面试硬通货**：“读入一组数 → 排序 → map 统计”是高频套路，不会 STL 寸步难行；\n4. **风格统一**：算法通过迭代器作用于所有容器，一套写法到处通用。\n\n## 核心用法\n\n**vector——会自动长大的数组**\n- `push_back(x)` 尾部追加、`pop_back()` 删尾部、`size()` 元素个数、`v[i]` 下标访问（O(1)）、`clear()` 清空；\n- 容量不足时自动扩容（通常翻倍申请新空间并整体搬移）。\n\n**string——专业存字符串**\n- 支持 `+` 拼接、`==`/`<` 整体比较、`substr(pos, len)` 截子串、`s[i]` 访问字符；\n- 比 C 风格的 `char[]` + `strlen/strcpy` 安全得多。\n\n**map——键值对字典**\n- `map<string,int> m;` 存“名字 → 数量”这类映射；`m[key]` 不存在时自动创建并初始化为 0，统计词频极方便；\n- 底层是红黑树：按 key 有序，插入/查找 O(log n)；不关心顺序可用 `unordered_map`（哈希表，均摊 O(1)）。\n\n**迭代器——容器的通用遥控器**\n- `v.begin()` 指向第一个元素，`v.end()` 指向最后一个的**下一个位置**（左闭右开区间）；\n- `it++` 前移、`*it` 取值；遍历写法 `for (auto it = v.begin(); it != v.end(); ++it)`，或更简洁的范围 for `for (auto& x : v)`；\n- 注意：扩容、`erase` 会让旧迭代器失效。\n\n**常用算法（头文件 `<algorithm>`）**\n- `sort(first, last, cmp)` 默认升序，传 `greater<int>()` 降序，也可传自定义比较函数；\n- `find` 查找、`count` 计数、`max_element`/`min_element` 求最值、`reverse` 翻转、`accumulate` 求和（在 `<numeric>`）。\n\n**与手写数据结构对比**\n\n| 对比项 | STL 容器 | 手写数据结构 |\n|---|---|---|\n| 开发效率 | 一行调用 | 几十上百行 |\n| 正确性 | 库级测试 | 易出边界 bug |\n| 性能 | 高度优化 | 通常更慢 |\n| 可读性 | 通用接口 | 需读实现才懂 |\n\n结论：工程与刷题优先 STL；只有性能定制、学习原理或面试手写环节才自己造轮子。\n\n## 代码示例\n\n```C++\n#include <iostream>\n#include <vector>\n#include <string>\n#include <map>\n#include <algorithm>\n#include <numeric>\n#include <functional>\nusing namespace std;\n\nint main() {\n    // 1. vector：动态数组 + 排序 + 常用算法\n    vector<int> v = {5, 3, 9, 1, 3};\n    sort(v.begin(), v.end());                 // 升序: 1 3 3 5 9\n    sort(v.begin(), v.end(), greater<int>()); // 降序: 9 5 3 3 1\n\n    for (auto it = v.begin(); it != v.end(); ++it)  // 迭代器遍历\n        cout << *it << \' \';\n    cout << \'\\n\';\n\n    cout << \"max=\" << *max_element(v.begin(), v.end())\n         << \" sum=\" << accumulate(v.begin(), v.end(), 0)\n         << \" has3=\" << (find(v.begin(), v.end(), 3) != v.end())\n         << \'\\n\';\n    reverse(v.begin(), v.end());              // 翻转\n\n    // 2. string\n    string s = \"hello\";\n    s += \" world\";                            // 拼接\n    cout << s.substr(0, 5) << \" len=\" << s.size() << \'\\n\';\n\n    // 3. map：统计词频\n    vector<string> words = {\"cpp\", \"stl\", \"cpp\"};\n    map<string, int> cnt;\n    for (auto& w : words) cnt[w]++;           // 不存在则自动置 0 再 ++\n    for (auto& [word, c] : cnt)               // 按 key 字典序输出\n        cout << word << \": \" << c << \'\\n\';\n\n    return 0;\n}\n```\n\n## 面试常问\n\n1. **vector 和普通数组的区别？**\n   vector 是动态数组，可自动扩容（申请更大空间并搬移元素），支持下标 O(1) 随机访问；数组大小固定不可增长。\n\n2. **map 和 unordered_map 怎么选？**\n   map 底层红黑树，key 有序、操作 O(log n)；unordered_map 底层哈希表，无序、均摊 O(1)。要有序遍历用 map，只做查找用 unordered_map。\n\n3. **迭代器什么时候失效？**\n   vector 扩容或 erase 后，指向旧位置的迭代器失效；删除时应写 `it = v.erase(it)` 接住返回的新迭代器。\n\n4. **sort 底层是什么实现？**\n   内省排序：快排为主，递归过深切换堆排，小区间用插入排序，兼顾平均速度与最坏 O(n log n)。\n\n5. **既然有 STL，为什么还要学手写数据结构？**\n   便于理解底层原理（扩容、哈希冲突、树旋转），应对性能定制和面试手写考察；日常开发优先用 STL。','generate://cpp-stl','cpp','generated',0,'2026-09-03 21:17:25','2026-09-03 21:17:25',0,NULL,NULL);
/*!40000 ALTER TABLE `knowledge` ENABLE KEYS */;
UNLOCK TABLES;
/*!40103 SET TIME_ZONE=@OLD_TIME_ZONE */;

/*!40101 SET SQL_MODE=@OLD_SQL_MODE */;
/*!40014 SET FOREIGN_KEY_CHECKS=@OLD_FOREIGN_KEY_CHECKS */;
/*!40014 SET UNIQUE_CHECKS=@OLD_UNIQUE_CHECKS */;
/*!40101 SET CHARACTER_SET_CLIENT=@OLD_CHARACTER_SET_CLIENT */;
/*!40101 SET CHARACTER_SET_RESULTS=@OLD_CHARACTER_SET_RESULTS */;
/*!40101 SET COLLATION_CONNECTION=@OLD_COLLATION_CONNECTION */;
/*!40111 SET SQL_NOTES=@OLD_SQL_NOTES */;

-- Dump completed on 2026-09-03 21:17:57
