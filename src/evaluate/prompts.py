"""评估模块提示词：把自由格式的评分文本兜底提取成结构化 JSON。"""

EXTRACT_PROMPT = """下面是一份讲解评估文本，正常应包含「总结复述、掌握度评分（X/10）、讲对的地方、讲错的地方、遗漏的知识点」，但格式可能不标准：

【评估文本】
{evaluation_text}

请提取结构化信息，只输出一个 JSON 对象，不要输出任何其他文字：
{{"score": 数字或 null（评分，0-10，提不到就 null）,
 "summary": "总结复述内容（没有就空字符串）",
 "correct": ["讲对的知识点，逐条"],
 "wrong": ["讲错的知识点，逐条"],
 "missed": ["遗漏的知识点，逐条"]}}

「无」这类表述输出为空数组。严格保证输出合法 JSON。
"""
