"""联网搜索补爬的提示词：生成检索词 + 过滤候选网页。

两处都要求模型只输出 JSON，调用方用宽容解析兜底（剥围栏/截花括号），
解析失败一律降级放弃——联网补爬是增强不是依赖，绝不阻断主流程。
"""

# 主题 → 搜索引擎检索词（1~2 条）
QUERY_GEN_PROMPT = """你是技术知识获取助手。请为学习主题生成 1-2 条用于搜索引擎的检索词，目标是找到高质量的学习资料（官方文档、系统教程、深度技术文章）：
- 检索词要具体、可读、利于命中权威来源；必要时补上英文术语或官方名称
- 主题本身已足够明确时，可以只给 1 条；不要给出口语化的长句
- 检索词偏向工程实践视角（架构、实现、用法），避免引向论文检索站
只输出 JSON：{"queries": ["..."]}"""

# 候选网页 → 按编号挑选有价值的页面
FILTER_PROMPT = """你是资料价值评审。学习主题是：{topic}{goal_line}

下面是搜索引擎返回的候选网页（序号 · 标题 · 摘要 · 网址）。请从中挑选对学习该主题真正有价值、且内容大概率充实完整的页面，最多选 {max_keep} 个：
- 优先官方文档、系统教程、深度技术文章
- 剔除视频、论坛问答碎片、目录/列表页、登录墙、下载页、广告，以及与主题关系不大的内容
- 论文摘要页 / PDF / arXiv 等预印本链接默认剔除（内容只有一段摘要，学不到东西）
- 与学习目标全景无关、只和子题字面沾边的内容不要——判断相关性以学习目标为准
- 内容明显互相重复的只留最优一条
按序号选择即可，不要修改或编造网址。只输出 JSON：
{{"keep": [{{"index": 1, "reason": "一句话理由"}}]}}
如果都不值得爬取，输出 {{"keep": []}}。

候选网页：
{candidates}"""
